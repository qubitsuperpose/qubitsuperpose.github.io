---
title: 14차시 14:Utility Scaled Quantum Computing 14
layout: single
classes: wide
categories:
  - Utility Scaled
toc: true # 이 포스트에서 목차를 활성화
toc_sticky: true # 목차를 고정할지 여부 (선택 사항)
---


# 14. Utility-scale experiment III
> Toshinari Itoko, Tamiya Onodera, Kifumi Numata (19 July 2024)
>
  [Download the pdf](https://ibm.ent.box.com/public/static/fw1538dogvyv0qbfqg8tan1k2fs27mfv.zip) of the original lecture. Note that some code snippets might become deprecated since these are static images.
>
  *Approximate QPU time to run this first experiment is 12 m 30 s. There is an additional experiment below that requires approximately 4 m.*
>
  (Note: this notebook might not evaluate in the time allowed on the Open Plan. Be sure to use quantum computing resources wisely.)


```python
import numpy as np
import rustworkx as rx

from qiskit import QuantumCircuit
from qiskit.visualization import plot_histogram
from qiskit.visualization import plot_gate_map
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager
from qiskit.providers import BackendV2
from qiskit.quantum_info import SparsePauliOp

from qiskit_ibm_runtime import QiskitRuntimeService
from qiskit_ibm_runtime import Sampler, Estimator, Batch, SamplerOptions
```


## 1. 서론

GHZ 상태를 간단히 복습하고, 그것에 `Sampler`를 적용했을 때 어떤 분포가 나올 것으로 예상되는지 살펴보겠습니다. 그런 다음 이번 레슨의 목표를 명확히 서술하겠습니다.

### 1.1 GHZ 상태
$n$ 큐비트에 대한 GHZ 상태(Greenberger-Horne-Zeilinger 상태)는 다음과 같이 정의됩니다.
<p>
$$
\frac{1}{\sqrt 2}(|0\rangle ^ {\otimes n}+ |1\rangle^ {\otimes n})
$$
</p>

당연하게도, 이는 다음의 양자 회로를 통해 6개의 큐비트로 생성할 수 있습니다.

```python
N = 6
qc = QuantumCircuit(N, N)

qc.h(0)
for i in range(N - 1):
    qc.cx(0, i + 1)

# qc.measure_all()
qc.barrier()
qc.measure(list(range(N)), list(range(N)))

qc.draw(output="mpl", idle_wires=False, scale=0.5)
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Futility-iii%2Fextracted-outputs%2F1bf914c5-e1c6-4664-99c2-69dea3961114-0.avif&w=1080&q=75)

```python
print("Depth:", qc.depth())
```

```
Depth: 7
```

깊이가 너무 크지는 않지만 이전 강의를 통해 더 잘할 수 있다는 것을 알고 있습니다. 백엔드를 선택하고 이 회로를 트랜스파일해 보겠습니다.

```python
service = QiskitRuntimeService()
backend = service.least_busy(operational=True, simulator=False)
backend.name
# or
# backend = service.least_busy(operational=True)
# backend.name
```

```python
pm = generate_preset_pass_manager(3, backend=backend)
qc_transpiled = pm.run(qc)
qc_transpiled.draw(output="mpl", idle_wires=False, fold=-1)
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Futility-iii%2Fextracted-outputs%2F2e003078-3707-44d9-86e8-bbc66b095e84-0.avif&w=3840&q=75)

```python
print("Depth:", qc_transpiled.depth())
print(
    "Two-qubit Depth:",
    qc_transpiled.depth(filter_function=lambda x: x.operation.num_qubits == 2),
)
```

```
Depth: 27
Two-qubit Depth: 11
```

다시 말하지만, 트랜스파일된 2큐비트 깊이는 그다지 크지 않습니다. 하지만 더 많은 큐비트에서 GHz 상태를 처리하려면 회로 최적화를 고려해야 할 것입니다. `Sampler`를 사용하여 실행하고 실제 양자 컴퓨터가 어떤 결과를 반환하는지 살펴보겠습니다.

```python
sampler = Sampler(mode=backend)
shots = 40000
job = sampler.run([qc_transpiled], shots=shots)
job_id = job.job_id()
print(job_id)
```

```python
job.status()
```

```python
job = service.job(job_id)
result = job.result()
plot_histogram(result[0].data.c.get_counts(), figsize=(30, 5))
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Futility-iii%2Fextracted-outputs%2F8ab5b37f-fb87-40a8-8e13-0c6cb3154f64-0.avif&w=3840&q=75)

이것은 6큐비트 GHz 회로의 결과입니다. 보시다시피, 모든 $\|0\rangle$ 상태와 모든 $\|1\rangle$ 상태가 지배적이지만, 오차가 상당합니다. Eagle 장치를 사용하여 올바른 상태가 나올 확률이 최소 50% 이상이 되도록 만들 수 있는 최대 규모의 GHz 회로를 만들어 보겠습니다.


### 1.2 목표

측정 시 **GHZ 상태의 충실도(fidelity) > 0.5**가 되도록 20 큐비트 이상의 GHZ 회로를 구성하라.
참고:

* Eagle 디바이스(`min_num_qubits=127`)를 사용해야 하며, shots 수는 40,000으로 설정해야 합니다.
* `execute_ghz_fidelity` 함수를 사용하여 GHZ 회로를 실행하고, `check_ghz_fidelity_from_jobs` 함수를 사용하여 충실도를 계산해야 합니다.

이는 지금까지 이 과정에서 배운 내용을 활용하는 독립적인 연습 문제로 의도되었습니다.

```python
def execute_ghz_fidelity(
    ghz_circuit: QuantumCircuit,  # Quantum circuit to create GHZ state (Circuit after Routing or without Routing), Classical register name is "c"
    physical_qubits: list[int],  # Physical qubits to represent GHZ state
    backend: BackendV2,
    sampler_options: dict | SamplerOptions | None = None,
):
    N_SHOTS = 40_000
    N = len(physical_qubits)
    base_circuit = ghz_circuit.remove_final_measurements(inplace=False)
    # M_k measurement circuits
    mk_circuits = []
    for k in range(1, N + 1):
        circuit = base_circuit.copy()
        # change measurement basis
        for q in physical_qubits:
            circuit.rz(-k * np.pi / N, q)
            circuit.h(q)
        mk_circuits.append(circuit)

    obs = SparsePauliOp.from_sparse_list(
        [("Z" * N, physical_qubits, 1)], num_qubits=backend.num_qubits
    )
    job_ids = []
    pm1 = generate_preset_pass_manager(1, backend=backend)
    org_transpiled = pm1.run(ghz_circuit)
    mk_transpiled = pm1.run(mk_circuits)
    with Batch(backend=backend):
        sampler = Sampler(options=sampler_options)
        sampler.options.twirling.enable_measure = True
        job = sampler.run([org_transpiled], shots=N_SHOTS)
        job_ids.append(job.job_id())
        # print(f"Sampler job id: {job.job_id()}, shots={N_SHOTS}")
        estimator = Estimator()  # TREX is applied as default
        estimator.options.dynamical_decoupling.enable = True
        estimator.options.execution.rep_delay = 0.0005
        estimator.options.twirling.enable_measure = True
        job2 = estimator.run([(circ, obs) for circ in mk_transpiled], precision=1 / 100)
        job_ids.append(job2.job_id())
        # print("Estimator job id:", job2.job_id())
        return [job.job_id(), job2.job_id()]

```

```python
def check_ghz_fidelity_from_jobs(
    sampler_job,
    estimator_job,
    num_qubits,
    shots=40_000,
):
    N = num_qubits
    sampler_result = sampler_job.result()
    counts = sampler_result[0].data.c.get_counts()
    all_zero = counts.get("0" * N, 0) / shots
    all_one = counts.get("1" * N, 0) / shots
    top3 = sorted(counts, key=counts.get, reverse=True)[:3]
    print(
        f"N={N}: |00..0>: {counts.get('0'*N, 0)}, |11..1>: {counts.get('1'*N, 0)}, |3rd>: {counts.get(top3[2], 0)} ({top3[2]})"
    )
    print(f"P(|00..0>)={all_zero}, P(|11..1>)={all_one}")

    estimator_result = estimator_job.result()
    non_diagonal = (1 / N) * sum(
        (-1) ** k * estimator_result[k - 1].data.evs for k in range(1, N + 1)
    )
    print(f"REM: Coherence (non-diagonal): {non_diagonal:.6f}")
    fidelity = 0.5 * (all_zero + all_one + non_diagonal)
    sigma = 0.5 * np.sqrt(
        (1 - all_zero - all_one) * (all_zero + all_one) / shots
        + sum(estimator_result[k].data.stds ** 2 for k in range(N)) / (N * N)
    )
    print(f"GHZ fidelity = {fidelity:.6f} ± {sigma:.6f}")
    if fidelity - 2 * sigma > 0.5:
        print("GME (genuinely multipartite entangled) test: Passed")
    else:
        print("GME (genuinely multipartite entangled) test: Failed")
    return {
        "fidelity": fidelity,
        "sigma": sigma,
        "shots": shots,
        "job_ids": [sampler_job.job_id(), estimator_job.job_id()],
    }
```

이 노트북에서는 16큐비트와 30큐비트를 사용하여 좋은 GHZ 상태를 생성하는 세 가지 전략을 적용해 보겠습니다. 이러한 접근 방식은 이전 수업에서 이미 알고 있는 전략을 기반으로 합니다.



## 2. 전략 1. 노이즈를 고려한 큐비트 선택

먼저 백엔드를 지정합니다. 특정 백엔드의 속성을 광범위하게 사용하게 되므로 `least_busy` 옵션을 사용하는 대신 하나의 백엔드를 지정하는 것이 좋습니다.

```python
backend = service.backend("ibm_strasbourg")  # eagle
twoq_gate = "ecr"
print(f"Device {backend.name} Loaded with {backend.num_qubits} qubits")
print(f"Two Qubit Gate: {twoq_gate}")
```

```
Device ibm_strasbourg Loaded with 127 qubits
Two Qubit Gate: ecr
```

우리는 여러 개의 2큐비트 게이트로 구성된 회로를 만들 것입니다. 이러한 2큐비트 게이트를 구현할 때 오류가 가장 적은 큐비트를 사용하는 것이 합리적입니다. 보고된 2큐비트 게이트 오류를 ​​기반으로 최적의 "큐비트 체인"을 찾는 것은 간단하지 않은 문제입니다. 하지만 최적의 큐비트를 결정하는 데 도움이 되는 몇 가지 함수를 정의할 수 있습니다.

```python
coupling_map = backend.target.build_coupling_map(twoq_gate)
G = coupling_map.graph
```

```python
def to_edges(path):  # create edges list from node paths
    edges = []
    prev_node = None
    for node in path:
        if prev_node is not None:
            if G.has_edge(prev_node, node):
                edges.append((prev_node, node))
            else:
                edges.append((node, prev_node))
        prev_node = node
    return edges


def path_fidelity(path, correct_by_duration: bool = True, readout_scale: float = None):
    """Compute an estimate of the total fidelity of 2-qubit gates on a path.
    If `correct_by_duration` is true, each gate fidelity is worsen by
    scale = max_duration / duration, that is, gate_fidelity^scale.
    If `readout_scale` > 0 is supplied, readout_fidelity^readout_scale
    for each qubit on the path is multiplied to the total fielity.
    The path is given in node indices form, for example, [0, 1, 2].
    An external function `to_edges` is used to obtain edge list, for example, [(0, 1), (1, 2)]."""
    path_edges = to_edges(path)
    max_duration = max(backend.target[twoq_gate][qs].duration for qs in path_edges)

    def gate_fidelity(qpair):
        duration = backend.target[twoq_gate][qpair].duration
        scale = max_duration / duration if correct_by_duration else 1.0
        # 1.25 = (d+1)/d with d = 4
        return max(0.25, 1 - (1.25 * backend.target[twoq_gate][qpair].error)) ** scale

    def readout_fidelity(qubit):
        return max(0.25, 1 - backend.target["measure"][(qubit,)].error)

    total_fidelity = np.prod(
        [gate_fidelity(qs) for qs in path_edges]
    )  # two qubits gate fidelity for each path
    if readout_scale:
        total_fidelity *= (
            np.prod([readout_fidelity(q) for q in path]) ** readout_scale
        )  # multiply readout fidelity
    return total_fidelity


def flatten(paths, cutoff=None):  # cutoff is for not making run time too large
    return [
        path
        for s, s_paths in paths.items()
        for t, st_paths in s_paths.items()
        for path in st_paths[:cutoff]
        if s < t
    ]
```

```python
N = 16  # Number of qubits to use in the GHZ circuit
num_qubits_in_chain = N
```

위에서 설명한 함수들을 이용하여 그래프의 모든 노드 쌍 사이에서 N개의 큐비트가 연결하는 모든 단순 경로를 찾습니다(참고: [all\_pairs\_all\_simple\_paths](https://www.rustworkx.org/apiref/rustworkx.all_pairs_all_simple_paths.html#rustworkx-all-pairs-all-simple-paths)).

그런 다음, 위에서 생성한 `path_fidelity` 함수를 사용하여 경로 충실도가 가장 높은 최적의 큐비트 체인을 찾습니다.

```python
from functools import partial

%%time
paths = rx.all_pairs_all_simple_paths(
    G.to_undirected(multigraph=False),
    min_depth=num_qubits_in_chain,
    cutoff=num_qubits_in_chain,
)
paths = flatten(paths, cutoff=25)  # If you have time, you could set a larger cutoff.
if not paths:
    raise Exception(
        f"No qubit chain with length={num_qubits_in_chain} exists in {backend.name}. Try smaller num_qubits_in_chain."
    )

print(f"Selecting the best from {len(paths)} candidate paths")

best_qubit_chain = max(
    paths, key=partial(path_fidelity, correct_by_duration=True, readout_scale=1.0)
)
assert len(best_qubit_chain) == num_qubits_in_chain
print(f"Predicted (best possible) process fidelity: {path_fidelity(best_qubit_chain)}")
```

```
Selecting the best from 6046 candidate paths
Predicted (best possible) process fidelity: 0.8929026784775056
CPU times: user 284 ms, sys: 10.9 ms, total: 295 ms
Wall time: 295 ms
```

```python
np.array(best_qubit_chain)
```

```
array([55, 49, 48, 47, 46, 45, 54, 64, 65, 66, 73, 85, 86, 87, 88, 89],
      dtype=uint64)
```

결합 맵 다이어그램에 분홍색으로 표시된 최적의 큐비트 체인을 그려 보겠습니다.

```python
qubit_color = []
for i in range(133):
    if i in best_qubit_chain:
        qubit_color.append("#ff00dd")  # pink
    else:
        qubit_color.append("#8c00ff")  # purple
plot_gate_map(
    backend, qubit_color=qubit_color, qubit_size=50, font_size=25, figsize=(6, 6)
)
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Futility-iii%2Fextracted-outputs%2Fa1c164a4-0.avif&w=1080&q=75)


### 2.1 최적의 큐비트 체인에 GHZ 회로를 구축합니다.

우리는 H 게이트를 먼저 적용할 큐비트를 체인의 중간에 있는 큐비트로 선택합니다. 이렇게 하면 회로의 깊이가 약 절반으로 줄어들 것입니다.

```python
ghz1 = QuantumCircuit(max(best_qubit_chain) + 1, N)
ghz1.h(best_qubit_chain[N // 2])
for i in range(N // 2, 0, -1):
    ghz1.cx(best_qubit_chain[i], best_qubit_chain[i - 1])
for i in range(N // 2, N - 1, +1):
    ghz1.cx(best_qubit_chain[i], best_qubit_chain[i + 1])
ghz1.barrier()  # for visualization
ghz1.measure(best_qubit_chain, list(range(N)))
ghz1.draw(output="mpl", idle_wires=False, scale=0.5, fold=-1)
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Futility-iii%2Fextracted-outputs%2F1336fba0-0.avif&w=1920&q=75)

```python
ghz1.depth()
```

```
10
```

```python
pm = generate_preset_pass_manager(1, backend=backend)
ghz1_transpiled = pm.run(ghz1)
ghz1_transpiled.draw(output="mpl", idle_wires=False, fold=-1)
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Futility-iii%2Fextracted-outputs%2F71874b6d-eb9b-4ff5-b673-944191f67010-0.avif&w=3840&q=75)

```python
print("Depth:", ghz1_transpiled.depth())
print(
    "Two-qubit Depth:",
    ghz1_transpiled.depth(filter_function=lambda x: x.operation.num_qubits == 2),
)
```

```
Depth: 27
Two-qubit Depth: 8
```

```python
opts = SamplerOptions()
```

```
res = execute_ghz_fidelity(
    ghz_circuit=ghz1,
    physical_qubits=best_qubit_chain,
    backend=backend,
    sampler_options=opts,
)
```

```python
job_s = service.job(res[0])  # Use your job id showed above.
job_e = service.job(res[1])
print(job_s.status(), job_e.status())
```

```
DONE DONE
```

위의 작업 상태가 '완료'로 변경된 후에 다음 셀을 실행하여 `check_ghz_fidelity_from_jobs` 함수를 사용한 결과를 표시하도록 주의하십시오.

```python
N = 16
# Check fidelity from job IDs
res = check_ghz_fidelity_from_jobs(
    sampler_job=job_s,
    estimator_job=job_e,
    num_qubits=N,
)
```

```
N=16: |00..0>: 153, |11..1>: 8681, |3rd>: 2262 (1111111111101111)
P(|00..0>)=0.003825, P(|11..1>)=0.217025
REM: Coherence (non-diagonal): 0.073809
GHZ fidelity = 0.147329 ± 0.002438
GME (genuinely multipartite entangled) test: Failed
```

```python
result = job_s.result()
plot_histogram(result[0].data.c.get_counts(), figsize=(30, 5))
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Futility-iii%2Fextracted-outputs%2F236126cb-0.avif&w=3840&q=75)

이 결과는 기준을 충족하지 못합니다. 다음 아이디어로 넘어가겠습니다.


## 3. 전략 2. 균형 잡힌 큐비트 트리

다음 단계는 균형 잡힌 큐비트 트리를 찾는 것입니다. 체인 방식 대신 트리 방식을 사용하면 회로 깊이가 낮아집니다. 그 전에, 연결 그래프에서 "나쁜" 판독 오류가 있는 노드와 "나쁜" 게이트 오류가 있는 에지를 제거합니다.

```python
BAD_READOUT_ERROR_THRESHOLD = 0.1
BAD_ECRGATE_ERROR_THRESHOLD = 0.1
bad_readout_qubits = [
    q
    for q in range(backend.num_qubits)
    if backend.target["measure"][(q,)].error > BAD_READOUT_ERROR_THRESHOLD
]
bad_ecrgate_edges = [
    qpair
    for qpair in backend.target["ecr"]
    if backend.target["ecr"][qpair].error > BAD_ECRGATE_ERROR_THRESHOLD
]
print("Bad readout qubits:", bad_readout_qubits)
print("Bad ECR gates:", bad_ecrgate_edges)
```

```
Bad readout qubits: [19, 28, 41, 72, 91, 114, 120]
Bad ECR gates: []
```

```python
g = backend.coupling_map.graph.copy().to_undirected()
g.remove_edges_from(
    bad_ecrgate_edges
)  # remove edge first (otherwise might fail with a NoEdgeBetweenNodes error)
g.remove_nodes_from(bad_readout_qubits)
```

불량 에지와 불량 큐비트를 제외하고 결합 맵 그래프를 그려봅시다.

```python
qubit_color = []
for i in range(133):
    if i in bad_readout_qubits:
        qubit_color.append("#000000")  # black
    else:
        qubit_color.append("#8c00ff")  # purple
line_color = []
for e in backend.target.build_coupling_map().get_edges():
    if e in bad_ecrgate_edges:
        line_color.append("#ffffff")  # white
    else:
        line_color.append("#888888")  # gray
plot_gate_map(
    backend,
    qubit_color=qubit_color,
    line_color=line_color,
    qubit_size=50,
    font_size=25,
    figsize=(6, 6),
)
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Futility-iii%2Fextracted-outputs%2F18825a87-4679-4027-af58-31efd24a9a60-0.avif&w=1080&q=75)

이전과 마찬가지로 16큐비트 GHz 상태를 생성하려고 합니다.

```python
N = 16
```

루트 노드에 해당하는 큐비트를 찾기 위해 `betweenness_centrality` 함수를 호출합니다. 매개 중심성 값이 가장 높은 노드가 그래프의 중심에 위치합니다. 참고: [https://www.rustworkx.org/tutorial/betweenness_centrality.html](https://www.rustworkx.org/tutorial/betweenness_centrality.html)

또는 수동으로 선택할 수도 있습니다.

```python
# central = 65 #Select the center node manually
c_degree = dict(rx.betweenness_centrality(g))
central = max(c_degree, key=c_degree.get)
central
```

```
66
```

루트 노드에서 시작하여 너비 우선 탐색(BFS)을 사용하여 트리를 생성합니다. 참고: [https://qiskit.org/ecosystem/rustworkx/apiref/rustworkx.bfs\_search.html#rustworkx-bfs-search](https://qiskit.org/ecosystem/rustworkx/apiref/rustworkx.bfs_search.html#rustworkx-bfs-search)


```python
class TreeEdgesRecorder(rx.visit.BFSVisitor):
    def __init__(self, N):
        self.edges = []
        self.N = N

    def tree_edge(self, edge):
        self.edges.append(edge)
        if len(self.edges) >= self.N - 1:
            raise rx.visit.StopSearch()


vis = TreeEdgesRecorder(N)
rx.bfs_search(g, [central], vis)
best_qubits = sorted(list(set(q for e in vis.edges for q in (e[0], e[1]))))
# print('Tree edges:', vis.edges)
```

```python
print("Qubits selected:", best_qubits)
```

```
Qubits selected: [54, 55, 63, 64, 65, 66, 67, 68, 69, 70, 73, 83, 84, 85, 86, 87]
```

분홍색으로 표시된 선택된 큐비트를 결합 맵 다이어그램에 나타내 보겠습니다.

```python
qubit_color = []
for i in range(133):
    if i in bad_readout_qubits:
        qubit_color.append("#000000")  # black
    elif i in best_qubits:
        qubit_color.append("#ff00dd")  # pink
    else:
        qubit_color.append("#8c00ff")  # purple
plot_gate_map(
    backend,
    qubit_color=qubit_color,
    line_color=line_color,
    qubit_size=50,
    font_size=25,
    figsize=(6, 6),
)
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Futility-iii%2Fextracted-outputs%2Fef18e037-a3ec-4370-bee7-bd71dbc12795-0.avif&w=1080&q=75)

큐비트의 트리 구조를 보여드리겠습니다.

```python
from rustworkx.visualization import graphviz_draw

tree = rx.PyDiGraph()
tree.extend_from_weighted_edge_list(vis.edges)
tree.remove_nodes_from([n for n in range(max(best_qubits) + 1) if n not in best_qubits])

graphviz_draw(tree, method="dot")
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Futility-iii%2Fextracted-outputs%2F8758d3a6-0.avif&w=1200&q=75)


```python
ghz2 = QuantumCircuit(max(best_qubits) + 1, N)

ghz2.h(tree.edge_list()[0][0])  # apply H-gate to the root node
# Apply CNOT from the root node to the each edge.
for u, v in tree.edge_list():
    ghz2.cx(u, v)
ghz2.barrier()  # for visualization
ghz2.measure(best_qubits, list(range(N)))
ghz2.draw(output="mpl", idle_wires=False, scale=0.5)
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Futility-iii%2Fextracted-outputs%2F3bc4b071-0.avif&w=1920&q=75)


```python
ghz2.depth()
```

```
8
```

```python
pm = generate_preset_pass_manager(1, backend=backend)
ghz2_transpiled = pm.run(ghz2)
ghz2_transpiled.draw(output="mpl", idle_wires=False, fold=-1)
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Futility-iii%2Fextracted-outputs%2Fc490e37a-309a-43e3-9b18-3be7c7d8a957-0.avif&w=3840&q=75)

```python
print("Depth:", ghz2_transpiled.depth())
print(
    "Two-qubit Depth:",
    ghz2_transpiled.depth(filter_function=lambda x: x.operation.num_qubits == 2),
)
```

```
Depth: 22
Two-qubit Depth: 6
```

이제 회로의 깊이는 체인 구조에서의 깊이보다 훨씬 얕아졌습니다.

```python
res = execute_ghz_fidelity(
    ghz_circuit=ghz2,
    physical_qubits=best_qubits,
    backend=backend,
    sampler_options=opts,
)
```

```python
job_s = service.job(res[0])  # Use your job id showed above.
job_e = service.job(res[1])
print(job_s.status(), job_e.status())
```

```
DONE DONE
```

```python
N = 16
# Check fidelity from job IDs
res = check_ghz_fidelity_from_jobs(
    sampler_job=job_s,
    estimator_job=job_e,
    num_qubits=N,
)
```

```
N=16: |00..0>: 9509, |11..1>: 10978, |3rd>: 1795 (1111110111111111)
P(|00..0>)=0.237725, P(|11..1>)=0.27445
REM: Coherence (non-diagonal): 0.606515
GHZ fidelity = 0.559345 ± 0.003188
GME (genuinely multipartite entangled) test: Passed
```

균형 잡힌 트리 구조로 기준을 성공적으로 통과했습니다!

```python
result = job_s.result()
plot_histogram(result[0].data.c.get_counts(), figsize=(30, 5))
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Futility-iii%2Fextracted-outputs%2Fcf8b4dac-0.avif&w=3840&q=75)

이제 더 큰 GHZ 상태, 즉 30큐비트 GHZ 상태를 만들어 보겠습니다.


### 3.1 N = 30

우리는 [Qiskit 패턴](https://quantum.cloud.ibm.com/docs/guides/intro-to-patterns) 프레임워크를 따를 것입니다.

* 1단계: 문제를 양자 회로와 연산자로 매핑
* 2단계: 대상 하드웨어에 맞게 최적화
* 3단계: 대상 하드웨어에서 실행
* 4단계: 결과 후처리

#### 3.1.1 1단계: 문제를 양자 회로 및 연산자로 매핑하고, 2단계: 목표 하드웨어에 맞게 최적화합니다.

여기서는 루트 노드를 수동으로 선택합니다.

```python
central = 62  # Select the center node manually
# c_degree = dict(rx.betweenness_centrality(g))
# central = max(c_degree, key=c_degree.get)
# central
```

```python
N = 30

vis = TreeEdgesRecorder(N)
rx.bfs_search(g, [central], vis)
best_qubits = sorted(list(set(q for e in vis.edges for q in (e[0], e[1]))))
print("Qubits selected:", best_qubits)
```

```
Qubits selected: [34, 35, 42, 43, 44, 45, 46, 47, 48, 53, 54, 56, 57, 58, 59, 60, 61, 62, 63, 64, 65, 66, 67, 68, 71, 73, 77, 84, 85, 86]
```

```python
qubit_color = []
for i in range(133):
    if i in bad_readout_qubits:
        qubit_color.append("#000000")
    elif i in best_qubits:
        qubit_color.append("#ff00dd")
    else:
        qubit_color.append("#8c00ff")
line_color = []
for e in backend.target.build_coupling_map().get_edges():
    if e in bad_ecrgate_edges:
        line_color.append("#ffffff")
    else:
        line_color.append("#888888")
plot_gate_map(
    backend,
    qubit_color=qubit_color,
    line_color=line_color,
    qubit_size=50,
    font_size=25,
    figsize=(6, 6),
)
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Futility-iii%2Fextracted-outputs%2F12d3ac00-7dfc-4734-9881-4e6385f2d880-0.avif&w=1080&q=75)

```python
from rustworkx.visualization import graphviz_draw

tree = rx.PyDiGraph()
tree.extend_from_weighted_edge_list(vis.edges)
tree.remove_nodes_from([n for n in range(max(best_qubits) + 1) if n not in best_qubits])

graphviz_draw(tree, method="dot")
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Futility-iii%2Fextracted-outputs%2F6afd6c54-b427-4f34-8c4c-285c0122aebd-0.avif&w=1920&q=75)

이 나무의 깊이는 5입니다.

```python
ghz3 = QuantumCircuit(max(best_qubits) + 1, N)

ghz3.h(tree.edge_list()[0][0])  # apply H-gate to the root node
# Apply CNOT from the root node to the each edge.
for u, v in tree.edge_list():
    ghz3.cx(u, v)
ghz3.barrier()  # for visualization
ghz3.measure(best_qubits, list(range(N)))
ghz3.draw(output="mpl", idle_wires=False, fold=-1)
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Futility-iii%2Fextracted-outputs%2Fd36cdb58-59fe-474e-9a31-a2d9e806ab59-0.avif&w=3840&q=75)

```python
ghz3.depth()
```

```
11
```

```python
pm = generate_preset_pass_manager(1, backend=backend)
ghz3_transpiled = pm.run(ghz3)
ghz3_transpiled.draw(output="mpl", idle_wires=False, fold=-1)
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Futility-iii%2Fextracted-outputs%2F46df385d-dd28-4fab-8344-6ef2fd53906d-0.avif&w=3840&q=75)

```python
print("Depth:", ghz3_transpiled.depth())
print(
    "Two-qubit Depth:",
    ghz3_transpiled.depth(filter_function=lambda x: x.operation.num_qubits == 2),
)
```

```
Depth: 31
Two-qubit Depth: 9
```



### 3.2 루트 노드를 수동으로 선택하세요

```python
central = 54

vis = TreeEdgesRecorder(N)
rx.bfs_search(g, [central], vis)
best_qubits = sorted(list(set(q for e in vis.edges for q in (e[0], e[1]))))
print("Qubits selected:", best_qubits)
```

```
Qubits selected: [23, 24, 25, 34, 35, 42, 43, 44, 45, 46, 47, 48, 49, 50, 54, 55, 60, 61, 62, 63, 64, 65, 66, 67, 68, 69, 73, 84, 85, 86]
```

```python
from rustworkx.visualization import graphviz_draw

tree = rx.PyDiGraph()
tree.extend_from_weighted_edge_list(vis.edges)
tree.remove_nodes_from([n for n in range(max(best_qubits) + 1) if n not in best_qubits])

graphviz_draw(tree, method="dot")
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Futility-iii%2Fextracted-outputs%2F5e2e2af5-34b5-4bb5-b393-18e87d56db08-0.avif&w=1920&q=75)

이 나무의 깊이는 6입니다.

```python
ghz3 = QuantumCircuit(max(best_qubits) + 1, N)

ghz3.h(tree.edge_list()[0][0])  # apply H-gate to the root node
# Apply CNOT from the root node to the each edge.
for u, v in tree.edge_list():
    ghz3.cx(u, v)
ghz3.barrier()  # for visualization
ghz3.measure(best_qubits, list(range(N)))
ghz3.draw(output="mpl", idle_wires=False, fold=-1)
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Futility-iii%2Fextracted-outputs%2F5d81d890-a939-455f-81a6-c7e671671ba5-0.avif&w=3840&q=75)

```python
ghz3.depth()
```

```
11
```

```python
pm = generate_preset_pass_manager(1, backend=backend)
ghz3_transpiled = pm.run(ghz3)
ghz3_transpiled.draw(output="mpl", idle_wires=False, fold=-1)
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Futility-iii%2Fextracted-outputs%2Fb9823b6d-a658-40ef-a509-f03e33534af6-0.avif&w=3840&q=75)

```python
print("Depth:", ghz3_transpiled.depth())
print(
    "Two-qubit Depth:",
    ghz3_transpiled.depth(filter_function=lambda x: x.operation.num_qubits == 2),
)
```

```
Depth: 30
Two-qubit Depth: 9
```

놀랍게도 트리 깊이는 5에서 6으로 증가했지만, 2큐비트 깊이는 9에서 8로 감소했습니다! 따라서 후자의 회로를 사용해 보겠습니다.

### 3.2.1 3단계: 대상 하드웨어에서 실행

```python
res = execute_ghz_fidelity(
    ghz_circuit=ghz3,
    physical_qubits=best_qubits,
    backend=backend,
    sampler_options=opts,
)
```

```python
job_s = service.job(res[0])  # Use your job id showed above.
job_e = service.job(res[1])
print(job_s.status(), job_e.status())
```

```
DONE DONE
```

#### 3.2.2 4단계: 결과 후처리

```python
N = 30
# Check fidelity from job IDs
res = check_ghz_fidelity_from_jobs(
    sampler_job=job_s,
    estimator_job=job_e,
    num_qubits=N,
)
```

```
N=30: |00..0>: 4, |11..1>: 218, |3rd>: 265 (111111111111111011111111111111)
P(|00..0>)=0.0001, P(|11..1>)=0.00545
REM: Coherence (non-diagonal): 0.187073
GHZ fidelity = 0.096312 ± 0.003254
GME (genuinely multipartite entangled) test: Failed
```

보시는 바와 같이, 이 결과는 기준을 충족하지 못했습니다.

```python
# It will take some time
result = job_s.result()
plot_histogram(result[0].data.c.get_counts(), figsize=(30, 5))
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Futility-iii%2Fextracted-outputs%2F66c87870-0.avif&w=3840&q=75)


## 4. 전략 3. 오류 억제 옵션을 사용하여 실행

Sampler V2에서 오류 억제 옵션을 설정할 수 있습니다. 자세한 내용은 [샘플러 노이즈 관리](/docs/guides/sampler-noise-management) 가이드와 [`ExecutionOptionsV2`](/docs/api/qiskit-ibm-runtime/options-execution-options-v2) API 참조를 참조하십시오.

```python
opts = SamplerOptions()
opts.dynamical_decoupling.enable = True
opts.execution.rep_delay = 0.0005
opts.twirling.enable_gates = True
```

```python
res = execute_ghz_fidelity(
    ghz_circuit=ghz3,
    physical_qubits=best_qubits,
    backend=backend,
    sampler_options=opts,
)
```

```python
job_s = service.job(res[0])  # Use your job id showed above.
job_e = service.job(res[1])
print(job_s.status(), job_e.status())
```

```
DONE DONE
```

```python
N = 30
```

```python
# Check fidelity from job IDs
res = check_ghz_fidelity_from_jobs(
    sampler_job=job_s,
    estimator_job=job_e,
    num_qubits=N,
)
```

```
N=30: |00..0>: 1459, |11..1>: 1543, |3rd>: 359 (111111111111111111111111111110)
P(|00..0>)=0.036475, P(|11..1>)=0.038575
REM: Coherence (non-diagonal): 0.165532
GHZ fidelity = 0.120291 ± 0.003369
GME (genuinely multipartite entangled) test: Failed
```

```python
# It will take some time
result = job_s.result()
plot_histogram(result[0].data.c.get_counts(), figsize=(30, 5))
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Futility-iii%2Fextracted-outputs%2F166856be-ad3a-40d6-837c-3eedda374891-0.avif&w=3840&q=75)

결과는 개선되었지만 아직 기준을 충족하지 못했습니다.

지금까지 세 가지 아이디어를 살펴보았습니다. 이 아이디어들을 조합하고 확장하거나, 더 나은 GHz 회로를 만들기 위해 자신만의 아이디어를 생각해낼 수 있습니다. 이제 목표를 다시 한번 살펴보겠습니다.


## 5. 목표 (요약)

20개 이상의 큐비트에 대해 GHZ 회로를 구성하여, 측정 결과가 다음 기준을 충족하도록 하세요: GHZ 상태의 fidelity > 0.5.

* Eagle 디바이스(예: `ibm_brisbane`)를 사용해야 하며, shots 수는 40,000으로 설정해야 합니다.
* `execute_ghz_fidelity` 함수를 사용하여 GHZ 회로를 실행하고, `check_ghz_fidelity_from_jobs` 함수를 사용하여 fidelity를 계산해야 합니다.
  기준을 충족하는 가장 큰 큐비트 수의 GHZ 회로를 찾아야 합니다. 아래에 코드를 작성하고, `check_ghz_fidelity_from_jobs` 함수로 결과를 보여주세요.

이제 앞선 내용과 동일한 GHZ 워크플로우를 Heron 디바이스에서 구현합니다. 이를 통해 Heron 프로세서의 레이아웃과 기능을 경험해볼 수 있습니다. 새로운 전략은 도입되지 않습니다.

*이 다음 실험을 실행하는 데 필요한 QPU 시간은 약 4분 40초입니다.*
