---
title: 13차시 5:Qiskit2.0 Certificate Exam Tutorial 5
layout: single
classes: wide
categories:
  - Qiskit Certificate
toc: true # 이 포스트에서 목차를 활성화
toc_sticky: true # 목차를 고정할지 여부 (선택 사항)
---

![](https://avatars.githubusercontent.com/u/41856731?s=96&v=4)
[출처:https://github.com/kibrahim757/qiskit_2x_certification_exam_tutorial](https://github.com/kibrahim757/qiskit_2x_certification_exam_tutorial/tree/main/Section_5_Use_Sampler_Primitive)

# 5. Use Sampler Primitives

## 1. Qiskit Primitives 에 대한 완전한 가이드: 이론 및 실습

이 종합적인 노트북은 이론적 기초부터 고급 오류 완화 기술을 통한 실제 구현까지, Qiskit 의 `Estimator` 및 `Sampler` primitives 에 대해 알아야 할 모든 것을 다룹니다.


**파트 1: 이론**
- Sampler primitive 가 무엇을 하며 왜 존재하는지
- 확률 분포 및 준확률 (quasi-probabilities)
- 오류 억제 기술의 이론적 기초

**파트 2: 실습**
- Estimator 와 Sampler 모두 시작하기
- 오류 완화를 위한 옵션 설정
- 동적 디커플링 (Dynamical Decoupling) 및 파울리 트월링 (Pauli Twirling) 구현
- 고급 resilience 옵션 사용 (ZNE, T-REx)
- 오류 완화 유무에 따른 결과 비교

### 1. 이론 이해

#### 1. Sampler Primitive 란 무엇인가?

`Sampler` 는 Qiskit 의 기초적인 primitives 중 하나입니다. 관측가능량의 기댓값을 계산하는 `Estimator` 와 달리, Sampler 는 측정 결과의 준확률 분포 (quasi-probability distribution) 를 근사하도록 설계되었습니다.

간단히 말해, 상태를 준비하는 양자 회로를 실행하면 계산 기준 (computational basis) 에서 측정 시 비트열 (예: `101` ) 이 생성됩니다. 이를 여러 번 (shots) 실행하면 카운트 히스토그램을 얻을 수 있습니다. Sampler 는 이 과정을 형식화하지만 다음과 같은 것을 가능하게 하는 추상화 계층을 추가합니다:

1.  **오류 완화 (Error Mitigation)** : 판독 또는 실행의 오류를 수정하거나 억제하도록 자동으로 작동합니다.
2.  **세션 관리 (Session Management)** : 여러 작업을 효율적으로 큐잉합니다 (클라우드 런타임 사용 시).
3.  **통합 인터페이스 (Unified Interface)** : 시뮬레이터와 실제 하드웨어 전체에서 일관되게 작동합니다.

#### 2. 확률 분포

샘플러는 기본적으로 기저 상태 $\|k\rangle$을 측정할 확률 $P(k)$을 추정합니다.

$P(k) = \|\langle k \| \psi \rangle\|^2 $

일반적인 `backend.run()`은 원시 카운트(예: `{'00': 50, '11': 50}`)를 반환하는 반면, 샘플러는 이러한 결과를 기본 확률 분포에서 추출한 샘플로 처리합니다.

**왜 "준 (Quasi)" 확률인가?**

"준확률 (quasi-probability)"이라는 용어를 들을 수 있습니다 (특히 Sampler V1 또는 특정 오류 완화 설정에서). 이는 오류 완화 후처리 (확률적 오류 취소와 같은) 로 인해 음수 값을 갖거나 1 이 아닌 숫자로 합계될 수 있는 분포를 의미합니다.

그러나 현재 구현 (V2) 및 기본 설정을 사용한 표준 사용에서는 양자 상태의 실제 확률 분포를 근사하는 샘플링된 비트열 (카운트) 또는 확률 질량 함수 (정규화된 카운트) 에 주로 중점을 둡니다.

#### 3. 오류 억제 및 완화 이론

`backend.run()` 대신 Sampler primitive 를 사용하는 주된 이유 중 하나는 고급 오류 처리 옵션에 액세스할 수 있기 때문입니다. Sampler 옵션을 통해 종종 노출되는 두 가지 주요 기술은 동적 디커플링 (Dynamical Decoupling) 과 파울리 트월링 (Pauli Twirling) 입니다.

**A. 동적 디커플링 (DD)**

**문제점:** 큐비트는 환경과의 상호 작용 (결맞음 소실, 일명 T1 및 T2 시간) 으로 인해 시간이 지남에 따라 양자 정보를 잃습니다. 이는 큐비트가 "유휴 (idle)" 상태일 때 (다른 큐비트가 게이트를 끝내기를 기다릴 때) 에도 발생합니다.

**이론:** 동적 디커플링은 유휴 큐비트에 일련의 펄스 (일반적으로 π-펄스, X 게이트와 같은) 를 적용합니다. 이 펄스는 큐비트 상태를 블로흐 구 (Bloch sphere) 에서 반대 축을 따라 반복적으로 뒤집습니다 ("스핀 에코 (spin echo)"와 같이).

**비유:** 트랙에서 달리는 러너를 상상해 보십시오. 바람으로 인해 약간 왼쪽으로 표류한다면, 180 도 뒤집으면 (트랙에 상대적으로) 오른쪽으로 표류하게 되어 원래 표류가 상쇄됩니다.

**Qiskit 에서:** Sampler 옵션에서 이를 활성화할 수 있으며, 트랜스파일러/스케줄러가 이러한 시퀀스를 유휴 창에 자동으로 삽입합니다.

**B. 파울리 트월링**

**문제점:** 체계적인 결맞음 오류 (예: 게이트가 항상 1 도만큼 과도하게 회전하는 경우) 는 구성적으로 추가될 수 있기 때문에 매우 손상적입니다.

**이론:** 트월링은 노이즈 게이트 (CNOT 와 같은) 를 무작위 파울리 게이트로 감싸는데, 이는 노이즈가 없다면 수학적으로 항등 연산으로 효과적으로 상쇄됩니다. 그러나 노이즈가 있는 경우, 이 무작위화는 "결맞음" 오류 (체계적 이동) 를 "비결맞음" 확률적 노이즈 (무작위 비트 뒤집기) 로 변환합니다.

**왜 이렇게 하는가?** 비결맞음 노이즈는 결맞음 오류보다 모델링하고 완화하기 훨씬 쉬운 "탈분극 채널 (depolarizing channel)"로 이어집니다. 이는 "나쁜" 예측 가능한 오류를 평균화되는 "무작위" 노이즈로 변환합니다.

### 2. 실제 구현

#### 1. Primitives 시작하기 (이상적인 로컬 시뮬레이션)

완벽하고 노이즈 없는 로컬 시뮬레이터 (`AerSimulator` ) 에서 `Estimator` 와 `Sampler` 를 실행하는 것으로 시작합니다. 이는 기본 워크플로우를 보여줍니다.

**Estimator 예시**

`Estimator` primitive 는 주어진 회로에 대한 관측가능량의 기댓값을 계산합니다. 이는 VQE 와 같은 많은 양자 알고리즘에서 기본적인 작업입니다.

```python
# 1. Import the local simulator backend
from qiskit_aer import AerSimulator
# We'll start with an ideal (noiseless) simulator.
backend = AerSimulator()

print(f"Using local backend: {backend.name}")
```

```python
from qiskit.circuit.library import qaoa_ansatz
from qiskit.quantum_info import SparsePauliOp
import numpy as np

# To make this runnable and easy to inspect, let's use a smaller 5-qubit circuit.
num_qubits = 5
entanglement = [(0, 1), (1, 2), (2, 3), (3, 4)]
observable = SparsePauliOp.from_sparse_list(
    [("ZZ", [i, j], 0.5) for i, j in entanglement],
    num_qubits=num_qubits,
)
circuit = qaoa_ansatz(observable, reps=2)

# The parameter values now need to match the smaller circuit's parameter count
param_values = np.random.rand(circuit.num_parameters)

print(f">>> Circuit has {circuit.num_parameters} parameters.")
print(f">>> Observable Paulis: {observable.paulis}")
```

```python
from qiskit.transpiler import generate_preset_pass_manager

# Transpiling the circuit for the backend's basis gates is good practice,
# though often optional for an ideal AerSimulator.
pm = generate_preset_pass_manager(optimization_level=1, backend=backend)
isa_circuit = pm.run(circuit)
isa_observable = observable.apply_layout(isa_circuit.layout)
print(f">>> Circuit ops (ISA-compliant): {isa_circuit.count_ops()}")
```

```python
# 2. Import the BackendEstimatorV2, which works with any local backend
from qiskit.primitives import BackendEstimatorV2 as Estimator

# Instantiate the Estimator with our local AerSimulator backend
estimator = Estimator(backend=backend)

# The run call takes a list of Primitives Unified Blocs (PUBs).
# Each PUB is a tuple of (circuit, observable, [parameter_values]).
job = estimator.run([(isa_circuit, isa_observable, param_values)])
print(f">>> Job ID: {job.job_id()}")
print(f">>> Job Status: {job.status()}")
```

```python
# 3. Crucially, let's actually look at the results!
result = job.result()
print(f"\n>>> Full Result Object: {result}")

# The result for the first PUB is at index 0
pub_result = result[0]
print(f"\n  > Expectation value (EV): {pub_result.data.evs}")
print(f"  > Standard deviation: {pub_result.data.stds}")
print(f"  > Metadata: {pub_result.metadata}")
```


**Sampler 예시**

`Sampler` primitive 는 회로 출력에서 비트열의 준확률 분포를 생성합니다. 이는 양자 시스템을 여러 번 측정하는 과정을 모방합니다.

```python
from qiskit.circuit.library import efficient_su2

# Again, using a smaller circuit for clarity
sampler_circuit = efficient_su2(5, entanglement="linear", reps=2)
sampler_circuit.measure_all()
sampler_param_values = np.random.rand(sampler_circuit.num_parameters)

# We can reuse the pass manager from before to transpile
sampler_isa_circuit = pm.run(sampler_circuit)
print(f">>> Circuit ops (ISA-compliant): {sampler_isa_circuit.count_ops()}")
```

```python
# 3. Import the BackendSamplerV2
from qiskit.primitives import BackendSamplerV2 as Sampler

sampler = Sampler(backend=backend)

# A Sampler PUB is a tuple of (circuit, [parameter_values], [shots]).
job = sampler.run([(sampler_isa_circuit, sampler_param_values)])
print(f">>> Job ID: {job.job_id()}")
print(f">>> Job Status: {job.status()}")
```

```python
# 4. Get and display the results
result = job.result()
print(f"\n>>> Full Result Object: {result}")

# Get results for the first (and only) PUB
pub_result = result[0]
print(
    f"\nFirst ten results for the 'meas' output register:\n{pub_result.data.meas.get_bitstrings()[:10]}"
)
```


#### 2. 오류 완화 옵션 설정

이제 이론을 이해했고 기본 사용법을 보았으니, 배운 오류 완화 기술을 구현해 보겠습니다.

`resilience_level` 및 `dynamical_decoupling` 과 같은 많은 고급 옵션은 `qiskit-ibm-runtime` 서비스의 기능입니다. 기본 `BackendEstimatorV2` 및 `BackendSamplerV2` 는 이를 직접 지원하지 않습니다.

(로컬 테스트/시뮬레이션 → BackendSamplerV2 / BackendEstimatorV2, 실제 하드웨어 → qiskit-ibm-runtime의 SamplerV2 / EstimatorV2 로 구분)

**그러나**, 노이즈 시뮬레이터와 함께 로컬 모드에서 런타임 primitives 를 사용하여 이러한 옵션의 효과를 테스트할 수 있습니다. 이를 통해 클라우드 계정이 없어도 API 를 학습하고 오류 완화가 어떻게 작동하는지 확인할 수 있습니다.


**오류 완화 예제를 위한 설정**

먼저, 실제 장치 (`FakeManilaV2` ) 의 노이즈 모델을 시뮬레이션하여 노이즈 백엔드를 생성해 보겠습니다. 이를 이후 모든 예제에 사용할 것입니다. 또한 이 특정 노이즈 백엔드에 대해 회로를 트랜스파일해야 합니다.

```python
# --- ROBUST IMPORT BLOCK for Fake Backends ---
try:
    from qiskit_ibm_runtime.fake_provider import FakeManilaV2
except ImportError:
    try:
        from qiskit.providers.fake_provider import FakeManilaV2
    except ImportError:
        FakeManilaV2 = None
# ---------------------------------------------

# We need the runtime Estimator, not the backend one, to access these options
from qiskit_ibm_runtime import EstimatorV2 as RuntimeEstimator

# This cell will only run if the import was successful
if FakeManilaV2:
    # 1. Create a realistic noisy backend
    # FakeManilaV2는 노이즈 데이터 제공자, AerSimulator는 그 데이터를 실제로 돌리는 고성능 엔진 
    noisy_backend = AerSimulator.from_backend(FakeManilaV2())

    # 2. Transpile our circuit specifically for this noisy backend's basis gates
    pm_noisy = generate_preset_pass_manager(optimization_level=1, backend=noisy_backend)
    noisy_isa_circuit = pm_noisy.run(circuit) # Using 'circuit' from the first Estimator example
    noisy_isa_observable = observable.apply_layout(noisy_isa_circuit.layout)

    print("Noisy backend and transpiled circuit are ready for mitigation examples.")
else:
    print("Skipping error mitigation examples because FakeManilaV2 is not available.")
    # Define placeholder variables so the notebook doesn't crash
    noisy_backend = None
    noisy_isa_circuit = None
    noisy_isa_observable = None
```


**일반적인 Sampler 옵션**

여기서는 `Sampler` 와 `Estimator` 모두에 공통적인 옵션 설정을 시연합니다.


**옵션: `default_shots`**

`default_shots` 는 모든 실행에 대한 shot 수를 설정하지만, `run()` 호출에서 재정의될 수 있습니다. Sampler 로 시연해 보겠습니다.

```python
from qiskit.primitives import BackendSamplerV2 as Sampler

# 1. Instantiate with a default
sampler_with_options = Sampler(backend=backend, options={"default_shots": 1000})

# 2. Run a job WITHOUT specifying shots in run() - it should use the default
job1 = sampler_with_options.run([(sampler_isa_circuit, sampler_param_values)])
result1 = job1.result()
print(f"Run 1 used the default shots: {result1[0].metadata['shots']}")

# 3. Run a job WITH shots specified in run() - this should override the default
job2 = sampler_with_options.run([(sampler_isa_circuit, sampler_param_values)], shots=123)
result2 = job2.result()
print(f"Run 2 used the overridden shots: {result2[0].metadata['shots']}")
```


#### 3. 동적 디커플링 구현

이제 앞서 배운 동적 디커플링 이론을 적용해 보겠습니다. 유휴 큐비트의 오류는 항등 연산 (펄스) 시퀀스를 추가하여 취소할 수 있습니다. 이는 런타임 전용 옵션입니다.

```python
if FakeManilaV2:
    # We must use the RuntimeEstimator for this option
    estimator_dd = RuntimeEstimator(mode=noisy_backend)

    # Set options for dynamic decoupling
    estimator_dd.options.dynamical_decoupling.enable = True
    estimator_dd.options.dynamical_decoupling.sequence_type = "XpXm" # A common sequence
    print(f"Dynamical Decoupling Enabled: {estimator_dd.options.dynamical_decoupling.enable}")
    print(f"Sequence Type: {estimator_dd.options.dynamical_decoupling.sequence_type}")

    # Run the job and show the result
    job_dd = estimator_dd.run([(noisy_isa_circuit, noisy_isa_observable, param_values)])
    result_dd = job_dd.result()
    print(f"  > Expectation value (with DD): {result_dd[0].data.evs}")
```


#### 4. 파울리 트월링 구현

파울리 트월링 (Pauli Twirling) 은 게이트를 무작위 파울리 연산자로 샌드위치하여 특정 유형의 결맞음 노이즈를 평균화하는 것을 포함합니다. 이는 런타임 `Estimator` 옵션입니다.

```python
if FakeManilaV2:
    estimator_twirl = RuntimeEstimator(mode=noisy_backend)
    
    # Set twirling options
    estimator_twirl.options.twirling.enable_gates = True
    estimator_twirl.options.twirling.num_randomizations = 16
    print(f"Gate Twirling Enabled: {estimator_twirl.options.twirling.enable_gates}")
    
    job_twirl = estimator_twirl.run([(noisy_isa_circuit, noisy_isa_observable, param_values)])
    result_twirl = job_twirl.result()
    print(f"  > Expectation value (with Twirling): {result_twirl[0].data.evs}")
```


#### 5. 고급 오류 완화: Resilience 옵션

`resilience` 옵션은 결과에 대한 노이즈의 영향을 줄이기 위한 런타임 `Estimator` 의 강력한 기능입니다.

**UserWarnings 에 대한 참고:** 로컬에서 실행할 경우 resilience_level이 아무런 효과가 없다는 경고가 표시될 수 있습니다. 하지만 실제 AerSimulator는 완화 조치를 수행하며, 결과는 개선된 것을 확인할 수 있습니다.

**제로 노이즈 외삽법 (ZNE)**

이 기술은 회로를 다른 증폭된 노이즈 수준에서 실행하고 결과를 제로 노이즈 한계로 외삽합니다.

```python
if FakeManilaV2:
    estimator_zne = RuntimeEstimator(mode=noisy_backend)
    
    # Set options for ZNE (level 0 disables presets)
    estimator_zne.options.resilience_level = 0
    estimator_zne.options.resilience.zne_mitigation = True
    print(f"ZNE Enabled: {estimator_zne.options.resilience.zne_mitigation}")

    job_zne = estimator_zne.run([(noisy_isa_circuit, noisy_isa_observable, param_values)])
    result_zne = job_zne.result()
    print(f"  > Expectation value (with ZNE): {result_zne[0].data.evs}")
```

**트월링 판독 오류 소거 (T-REx)**

이 방법은 구체적으로 큐비트의 최종 측정 중에 발생하는 오류를 대상으로 합니다.

```python
if FakeManilaV2:
    estimator_trex = RuntimeEstimator(mode=noisy_backend)
    
    # Set options for T-REx (level 0 disables presets)
    estimator_trex.options.resilience_level = 0
    estimator_trex.options.resilience.measure_mitigation = True
    print(f"Measurement Mitigation (T-REx) Enabled: {estimator_trex.options.resilience.measure_mitigation}")
    
    job_trex = estimator_trex.run([(noisy_isa_circuit, noisy_isa_observable, param_values)])
    result_trex = job_trex.result()
    print(f"  > Expectation value (with T-REx): {result_trex[0].data.evs}")
```

#### 6. 최종 비교: `resilience_level` 프리셋 사용

`resilience_level` 은 여러 오류 완화 기술을 결합하는 편리한 프리셋입니다.

*   **레벨 0:** 완화 없음.
*   **레벨 1:** ZNE + T-REx. 이는 좋은 일반적인 출발점입니다.

완화 없이 실행한 결과와 `resilience_level=1` 로 실행한 결과를 실제 이상적인 값과 비교해 보겠습니다.

```python
if FakeManilaV2:
    # --- Ideal (noiseless) result for a baseline ---
    ideal_backend = AerSimulator()
    estimator_ideal = RuntimeEstimator(mode=ideal_backend)
    result_ideal = estimator_ideal.run([(isa_circuit, isa_observable, param_values)]).result()

    # --- Estimator WITHOUT any mitigation ---
    estimator_no_mitigation = RuntimeEstimator(mode=noisy_backend)
    estimator_no_mitigation.options.resilience_level = 0
    job_no_mitigation = estimator_no_mitigation.run([(noisy_isa_circuit, noisy_isa_observable, param_values)])
    result_no_mitigation = job_no_mitigation.result()

    # --- Estimator WITH Level 1 mitigation ---
    estimator_with_mitigation = RuntimeEstimator(mode=noisy_backend)
    estimator_with_mitigation.options.resilience_level = 1
    job_with_mitigation = estimator_with_mitigation.run([(noisy_isa_circuit, noisy_isa_observable, param_values)])
    result_with_mitigation = job_with_mitigation.result()

    print("\n--- FINAL COMPARISON ---")
    print(f"  > Expectation value (Ideal, Noiseless): {result_ideal[0].data.evs}")
    print(f"  > Expectation value (Noisy, NO mitigation): {result_no_mitigation[0].data.evs}")
    print(f"  > Expectation value (Noisy, WITH mitigation, resilience_level=1): {result_with_mitigation[0].data.evs}")
```

### 3. 요약

이 종합적인 노트북에서 다음을 다뤘습니다:

**이론:**
1.  **Sampler 목적:** 출력 확률 분포를 샘플링한다는 것을 배웠습니다.
2.  **동적 디커플링 이론:** 펄스 시퀀스로 유휴 큐비트를 재초점하는 것이 어떻게 결맞음 소실을 취소하는지.
3.  **파울리 트월링 이론:** 결맞음 오류를 무작위화하여 비결맞음 확률적 노이즈로 변환하는 방법.

**실습:**
1.  **기본 Primitives:** 이상적인 시뮬레이터에서 Estimator 와 Sampler 를 모두 설정하고 실행했습니다.
2.  **옵션 구성:** `default_shots` 및 기타 옵션을 설정하는 방법을 배웠습니다.
3.  **오류 완화 구현:**
    - 동적 디커플링 (DD)
    - 파울리 트월링
    - 제로 노이즈 외삽법 (ZNE)
    - 트월링 판독 오류 소거 (T-REx)
4.  **Resilience 레벨:** 오류 완화 유무에 따른 결과를 비교했습니다.

### 4. Practice Questions

1.What is the primary output calculated by the Sampler primitive?

A) Expectation values of observables

B) Quasi-probability distributions of measurement outcomes

C) The statevector of the quantum system

D) The unitary matrix of the circuit

<Details><summary>정답:</summary>
<br/>
B) Quasi-probability distributions of measurement outcomes
</Details>
<br>

2.Which error mitigation technique suppresses decoherence errors on idle qubits by applying a sequence of pulses (like X gates)?

A) Pauli Twirling

B) Zero Noise Extrapolation (ZNE)

C) Dynamical Decoupling (DD)

D) Probabilistic Error Cancellation (PEC)

<Details><summary></summary>
<br/>
C) Dynamical Decoupling (DD)
</Details>
<br>

3.In the Qiskit Runtime Estimator V2, how would you explicitly enable Zero Noise Extrapolation (ZNE) if you are not using a resilience level preset?

A) `options.resilience.zne_mitigation = True`

B) `options.zne = True`

C) `options.error_mitigation.zne = True`

D) `options.resilience_level = 'ZNE'`

<Details><summary>정답:</summary>
<br/>
A) `options.resilience.zne_mitigation = True`
</Details>


## 2. Qiskit Primitives V1 에서 V2 로 마이그레이션하는 가이드

이 노트북은 Qiskit 의 V1 Primitives 에서 현대적인 V2 Primitives ( `EstimatorV2` , `SamplerV2` ) 로 코드를 업데이트하는 실용적인 실무 가이드를 제공합니다.

**참고:** V1 Primitives 는 최근 Qiskit 버전에서 사용 중단 (deprecated) 되었습니다. 이 가이드는 로컬에서 `AerSimulator` 로 실행할 수 있는 작동 중인 V2 코드 옆에 주석 처리된 예시로 V1 코드 패턴을 보여줍니다.

### 1. 설정: Imports 및 백엔드

먼저 V2 primitives 에 필요한 모든 것을 import 합니다. 모든 예제는 로컬 `AerSimulator` 에서 실행됩니다.

```python
# General Qiskit tools
from qiskit import QuantumCircuit
from qiskit.quantum_info import SparsePauliOp
import numpy as np

# The local simulator backend we will use for all examples
from qiskit_aer import AerSimulator

# --- V2 Primitives (Current/Recommended) ---
from qiskit_ibm_runtime import EstimatorV2 as Estimator
from qiskit_ibm_runtime import SamplerV2 as Sampler

# Tool to pretty-print options dictionaries
from dataclasses import asdict

# Define a local backend instance to use throughout
local_backend = AerSimulator()

print("Imports complete and local backend is ready.")
```

```python
# Helper function to create a Bell state circuit
def create_bell_circuit():
    """Create a Bell state (maximally entangled state) circuit."""
    qc = QuantumCircuit(2)
    qc.h(0)  # Apply Hadamard to qubit 0
    qc.cx(0, 1)  # Apply CNOT with control=0, target=1
    return qc

# Test it
bell = create_bell_circuit()
print(bell)
```



### 2. `run()` 메서드: 회로, 관측가능량 및 파라미터

V1 에서 V2 로의 가장 중요한 변경 사항은 `run()` 메서드 입력의 구조입니다.

#### 1. 주요 차이점:
- **V1:** `circuits` , `observables` 및 `parameter_values` 에 대해 별도의 인수를 사용했습니다.
- **V2:** 회로와 해당 관측가능량 및 파라미터를 그룹화하는 튜플인 PUBs(Primitive Unified Blocs) 의 단일 목록을 사용합니다.

#### 2. Estimator: V1 → V2 마이그레이션

간단한 Bell 회로 기댓값 계산을 마이그레이션하는 방법을 살펴보겠습니다.

```python
print("=" * 60)
print("V1 CODE (Deprecated - shown for reference):")
print("=" * 60)
print("""
# V1 Estimator - OLD WAY (Don't use this anymore)
from qiskit_ibm_runtime import Estimator

circuit = create_bell_circuit()
observable = SparsePauliOp("ZZ")

estimator = Estimator(backend=local_backend)
# V1 used separate keyword arguments
job = estimator.run(
    circuits=[circuit],
    observables=[observable]
)
result = job.result()
print(f"Expectation Value: {result.values[0]}")
""")

print("\n" + "=" * 60)
print("V2 CODE (Current - Working Example):")
print("=" * 60)
```

```python
# V2 Estimator - NEW WAY (Use this!)
from qiskit_ibm_runtime import EstimatorV2 as Estimator

circuit = create_bell_circuit()
observable = SparsePauliOp("ZZ")

estimator = Estimator(mode=local_backend)

# V2 uses PUBs: a list of tuples (circuit, observable, [optional params])
job = estimator.run(pubs=[(circuit, observable)])
result = job.result()

# V2 results are indexed by PUB, and data is in .data attribute
pub_result = result[0]
print(f"V2 Job ID: {job.job_id()}")
print(f"V2 Expectation Value: {pub_result.data.evs}")
print(f"V2 Standard Deviation: {pub_result.data.stds}")
```


#### 3. Sampler: V1 → V2 마이그레이션

Sampler 도 패턴은 동일합니다.

```python
print("=" * 60)
print("V1 CODE (Deprecated - shown for reference):")
print("=" * 60)
print("""
# V1 Sampler - OLD WAY
from qiskit_ibm_runtime import Sampler

circuit = create_bell_circuit()
circuit.measure_all()

sampler = Sampler(backend=local_backend)
# V1 used separate keyword argument
job = sampler.run(circuits=[circuit])
result = job.result()
print(f"Quasi-distribution: {result.quasi_dists[0]}")
""")

print("\n" + "=" * 60)
print("V2 CODE (Current - Working Example):")
print("=" * 60)
```

```python
# V2 Sampler - NEW WAY (Use this!)
from qiskit_ibm_runtime import SamplerV2 as Sampler

circuit = create_bell_circuit()
circuit.measure_all()

sampler = Sampler(mode=local_backend)

# V2 uses PUBs: a list of tuples (circuit, [optional params], [optional shots])
# Single circuit with no params - note the comma to make it a tuple
job = sampler.run(pubs=[(circuit,)])
result = job.result()

# V2 results are indexed by PUB
pub_result = result[0]
print(f"V2 Job ID: {job.job_id()}")
print(f"V2 Bitstring counts: {pub_result.data.meas.get_counts()}")
print(f"V2 First 5 bitstrings: {pub_result.data.meas.get_bitstrings()[:5]}")
```

### 3. 서로 다른 파라미터를 가진 여러 작업 처리

V2 PUB 시스템은 다양한 작업을 실행하는 것을 훨씬 쉽게 만듭니다. 각 회로에 대해 서로 다른 shots, 파라미터 또는 관측가능량을 지정할 수 있습니다.

#### 1. 예시: 서로 다른 Shot 수를 가진 두 회로

```python
# Create two different circuits
circuit1 = create_bell_circuit()
circuit1.measure_all()

circuit2 = create_bell_circuit()
circuit2.ry(np.pi / 4, 0)  # Add extra rotation
circuit2.measure_all()

# V2 PUB format for Sampler: (circuit, parameter_values, shots)
# We can specify different shots for each circuit!
pub1 = (circuit1, None, 100)   # Circuit 1 with 100 shots
pub2 = (circuit2, None, 500)   # Circuit 2 with 500 shots

sampler = Sampler(mode=local_backend)
job = sampler.run(pubs=[pub1, pub2])
result = job.result()

# Results are indexed corresponding to the input PUBs
print(f"Result for Circuit 1 (shots={result[0].metadata['shots']}):\n  {result[0].data.meas.get_counts()}")
print(f"\nResult for Circuit 2 (shots={result[1].metadata['shots']}):\n  {result[1].data.meas.get_counts()}")
```

#### 2. 예시: 여러 관측가능량을 가진 하나의 회로

V2 는 동일한 회로에서 여러 관측가능량을 측정하는 것을 쉽게 만듭니다.

```python
circuit = create_bell_circuit()

# Define multiple observables to measure
obs1 = SparsePauliOp("ZZ")
obs2 = SparsePauliOp("XX")
obs3 = SparsePauliOp("YY")

# V2 PUB format: (circuit, [list of observables])
estimator = Estimator(mode=local_backend)
job = estimator.run(pubs=[(circuit, [obs1, obs2, obs3])])
result = job.result()

pub_result = result[0]
print("Expectation values for multiple observables:")
print(f"  ZZ: {pub_result.data.evs[0]}")
print(f"  XX: {pub_result.data.evs[1]}")
print(f"  YY: {pub_result.data.evs[2]}")
```

### 4. 결과 액세스 (출력 구조 변경)

결과 구조가 크게 변경되었습니다:

#### 1. V1 결과:

```python
# Estimator V1
result.values[0] # 기댓값 배열
result.metadata[0] # 작업 0 에 대한 메타데이터

# Sampler V1 
result.quasi_dists[0] # 준확률 분포
```

#### 2. V2 결과:
```python
# Estimator V2
result[0].data.evs # PUB 0 에 대한 기댓값
result[0].data.stds # PUB 0 에 대한 표준 편차
result[0].metadata # PUB 0 에 대한 메타데이터

# Sampler V2
result[0].data.meas.get_counts() # 측정 카운트
result[0].data.meas.get_bitstrings() # 비트열 배열
```

#### 3. 예시: V2 Sampler 출력을 V1 스타일 QuasiDistribution 으로 변환

```python
# Run a V2 Sampler job
circuit = create_bell_circuit()
circuit.measure_all()

sampler = Sampler(mode=local_backend)
job = sampler.run([(circuit,)], shots=1024)
result = job.result()

# V2 result structure
pub_result = result[0]
shots = pub_result.metadata['shots']
counts = pub_result.data.meas.get_counts()

print(f"V2 Counts (dict of bitstrings): {counts}")
print(f"Total shots: {shots}")

# Convert to V1-style quasi-distribution (dict with integer keys)
v1_quasi_dist = {int(bitstring, 2): count/shots for bitstring, count in counts.items()}
print(f"\nV1-style QuasiDistribution (for migration): {v1_quasi_dist}")
```

### 5. 옵션 설정

옵션 구성은 V2 에서 간소화되었습니다.

#### 1. V1 접근 방식 (사용 중단):
```python
from qiskit_ibm_runtime import Options

options = Options()
options.resilience_level = 1
options.execution.shots = 4000
estimator = Estimator(backend=backend, options=options)

# 또는 나중에 업데이트:
estimator.set_options(resilience_level=2)
```

#### 2. V2 접근 방식 (현재):
primitive 에 직접 설정할 수 있는 내장 `.options` 속성이 있습니다.

```python
# Method 1: Pass options as a dictionary during initialization
estimator = Estimator(
    mode=local_backend,
    options={"resilience_level": 1, "default_shots": 2000}
)
print(f"Resilience level: {estimator.options.resilience_level}")
print(f"Default shots: {estimator.options.default_shots}")
```

```python
# Method 2: Set attributes directly (enables auto-complete in IDEs)
estimator = Estimator(mode=local_backend)
estimator.options.resilience_level = 1
estimator.options.default_shots = 4000

print(f"Resilience level: {estimator.options.resilience_level}")
print(f"Default shots: {estimator.options.default_shots}")
```

```python
# Method 3: Use update() for bulk updates
estimator = Estimator(mode=local_backend)
estimator.options.update(
    resilience_level=0,
    default_shots=1024
)

print(f"Resilience level: {estimator.options.resilience_level}")
print(f"Default shots: {estimator.options.default_shots}")

# View all options as a dictionary
print(f"\nAll options:\n{asdict(estimator.options)}")
```

### 6. 요약: 마이그레이션 체크리스트

#### 1. EstimatorV2 로 마이그레이션하는 단계:

1. **imports 업데이트:**
    ```python
    # 이전: from qiskit_ibm_runtime import Estimator
    # 새로운:
    from qiskit_ibm_runtime import EstimatorV2 as Estimator
    ```

2. **Options import 제거** (더 이상 필요하지 않음)

3. **backend 파라미터 변경:**
    ```python
    # 이전: estimator = Estimator(backend=backend)
    # 새로운:
    estimator = Estimator(mode=backend)
    ```

4. **옵션 설정 업데이트:**
    ```python
    # 이전: estimator.set_options(resilience_level=1)
    # 새로운:
    estimator.options.resilience_level = 1
    ```

5. **입력을 PUBs 로 그룹화:**
    ```python
    # 이전: job = estimator.run(circuits=[circuit], observables=[obs])
    # 새로운:
    job = estimator.run(pubs=[(circuit, obs)])
    ```

6. **결과 액세스 업데이트:**
    ```python
    # 이전: result.values[0]
    # 새로운:
    result[0].data.evs
    ```

#### 2. SamplerV2 로 마이그레이션하는 단계:

1. **imports 업데이트:**
    ```python
    from qiskit_ibm_runtime import SamplerV2 as Sampler
    ```

2. **backend 파라미터 변경:**
    ```python
    sampler = Sampler(mode=backend)
    ```

3. **옵션 업데이트:**
    ```python
    sampler.options.default_shots = 4096
    ```

4. **입력을 PUBs 로 그룹화:**
    ```python
    # 이전: job = sampler.run(circuits=[circuit])
    # 새로운:
    job = sampler.run(pubs=[(circuit,)]) # 쉼표에 주의!
    ```

5. **결과 액세스 업데이트:**
    ```python
    # 이전: result.quasi_dists[0]
    # 새로운:
    result[0].data.meas.get_counts()
    # 또는
    result[0].data.meas.get_bitstrings()
    ```

### 7. 결론

V2 Primitives 는 다음을 제공합니다:
- **PUB 시스템을 통한 더 많은 유연성**
- **내장 옵션을 통한 더 깔끔한 API**
- **다양한 워크로드 (회로당 다른 shots, 관측가능량, 파라미터) 에 대한 더 나은 지원**
- **향상된 성능 및 오류 완화 기능**

이 노트북의 모든 코드는 클라우드 액세스 없이 로컬에서 실행되므로 마이그레이션을 연습하고 테스트하기 쉽습니다!

### 7. Practice Questions

1.In V2 Primitives, what is the required structure for the input to the `run()` method?

A) Separate lists for circuits and observables (e.g., `circuits=[...], observables=[...]`)

B) A single list of PUBs (Primitive Unified Blocs), where each PUB is a tuple

C) A dictionary mapping circuits to parameters

D) A standard QuantumCircuit object only

<Details><summary>정답:</summary>
<br/>
B) A single list of PUBs (Primitive Unified Blocs), where each PUB is a tuple
</Details>
<br>

2.Given a result object `pub_result` from a V2 Sampler, how do you access the dictionary of measurement counts?

A) `pub_result.get_counts()`

B) `pub_result.data.meas.get_counts()`

C) `pub_result.quasi_dists[0]`

D) `pub_result.counts`

<Details><summary>정답:</summary>
<br/>
B) `pub_result.data.meas.get_counts()`
</Details>
<br>

3.What is a key advantage of the V2 PUB (Primitive Unified Bloc) system regarding shots?

A) You can specify a different number of shots for each circuit in the same job

B) It automatically calculates the optimal number of shots

C) It allows infinite shots

D) It ignores the shot count and uses the backend default always

<Details><summary>정답:</summary>
<br/>
A) You can specify a different number of shots for each circuit in the same job
</Details>