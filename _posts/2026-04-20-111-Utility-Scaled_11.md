---
title: 14차시 11:Utility Scaled Quantum Computing 11
layout: single
classes: wide
categories:
  - Utility Scaled
toc: true # 이 포스트에서 목차를 활성화
toc_sticky: true # 목차를 고정할지 여부 (선택 사항)
---


# 11. Quantum noise and error mitigation

> Toshinari Itoko (28 June 2024)
>
  [Download the pdf](https://ibm.ent.box.com/public/static/a0zgies7bh91hm2lwev9o0bfeybxc6n6.zip) of the original lecture. Note that some code snippets might become deprecated since these are static images.
>
  *Approximate QPU time to run this experiment is 1 m 40 s.*

## 1. 서론

이번 강의에서는 양자 컴퓨터에서 발생하는 노이즈와 이를 완화하는 방법에 대해 살펴보겠습니다. 우선, 실제 양자 컴퓨터의 노이즈 프로파일을 활용하는 등 다양한 방식으로 노이즈를 시뮬레이션할 수 있는 시뮬레이터를 사용하여 노이즈의 영향을 살펴보는 것으로 시작하겠습니다. 그다음에는 노이즈가 본질적으로 존재하는 실제 양자 컴퓨터로 넘어가겠습니다. 또한 제로 노이즈 외삽(ZNE) 및 게이트 트월링과 같은 기법들의 조합을 포함한 오류 완화의 효과에 대해 살펴보겠습니다.

일부 패키지를 로드하는 것부터 시작하겠습니다.

```python
# !pip install qiskit qiskit_aer qiskit_ibm_runtime
# !pip install jupyter
# !pip install matplotlib pylatexenc
```

## 2. 오류 완화 없는 노이즈 시뮬레이션

Qiskit Aer는 양자 컴퓨팅을 위한 고전 시뮬레이터입니다. 양자 회로의 이상적인 실행뿐만 아니라 노이즈가 포함된 실행도 시뮬레이션할 수 있습니다. 이 노트북에서는 Qiskit Aer를 사용하여 노이즈 시뮬레이션을 실행하는 방법을 보여줍니다.

1. 노이즈 모델 구축
2. 노이즈 모델을 사용하여 노이즈 sampler(시뮬레이터) 구축
3. 노이즈 sampler에서 양자 회로 실행

```
noise_model = NoiseModel()
...
noisy_sampler = Sampler(options={"backend_options": {"noise_model": noise_model}})
job = noisy_sampler.run([circuit])
```

#### 2.1 테스트 회로 구축

본 연구에서는 X개의 게이트를 `d`번(`d`=0 ... 100) 반복하는 간단한 1큐비트 회로를 고려하고, 관측량 `Z`를 측정합니다.

```python
from qiskit.circuit import QuantumCircuit

MAX_DEPTH = 100
circuits = []
for d in range(MAX_DEPTH + 1):
    circ = QuantumCircuit(1)
    for _ in range(d):
        circ.x(0)
        circ.barrier(0)
    circ.measure_all()
    circuits.append(circ)

display(circuits[3].draw(output="mpl"))
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Ferror-mitigation%2Fextracted-outputs%2Fb4863c66-0.avif&w=1920&q=75)


```python
from qiskit.quantum_info import SparsePauliOp

obs = SparsePauliOp.from_list([("Z", 1.0)])
obs
```

```
SparsePauliOp(['Z'],
              coeffs=[1.+0.j])
```



#### 2.2 노이즈 모델 구축

노이즈가 포함된 시뮬레이션을 수행하려면 `NoiseModel`을 지정해야 합니다. 이 섹션에서는 `NoiseModel`을 구축하는 방법을 설명합니다.

먼저 노이즈 모델에 추가할 양자(또는 판독) 오류를 정의해야 합니다.

```python
from qiskit_aer.noise.errors import (
    coherent_unitary_error,
    amplitude_damping_error,
    ReadoutError,
)
from qiskit.circuit.library import RXGate

# Coherent (unitary) error: Over X-rotation error
# https://qiskit.github.io/qiskit-aer/stubs/qiskit_aer.noise.coherent_unitary_error.html#qiskit_aer.noise.coherent_unitary_error
OVER_ROTATION_ANGLE = 0.05
coherent_error = coherent_unitary_error(RXGate(OVER_ROTATION_ANGLE).to_matrix())

# Incoherent error: Amplitude dumping error
# https://qiskit.github.io/qiskit-aer/stubs/qiskit_aer.noise.amplitude_damping_error.html#qiskit_aer.noise.amplitude_damping_error
AMPLITUDE_DAMPING_PARAM = 0.02  # in [0, 1] (0: no error)
incoherent_error = amplitude_damping_error(AMPLITUDE_DAMPING_PARAM)

# Readout (measurement) error: Readout error
# https://qiskit.github.io/qiskit-aer/stubs/qiskit_aer.noise.ReadoutError.html#qiskit_aer.noise.ReadoutError
PREP0_MEAS1 = 0.03  # P(1|0): Probability of preparing 0 and measuring 1
PREP1_MEAS0 = 0.08  # P(0|1): Probability of preparing 1 and measuring 0
readout_error = ReadoutError(
    [[1 - PREP0_MEAS1, PREP0_MEAS1], [PREP1_MEAS0, 1 - PREP1_MEAS0]]
)
```

```python
from qiskit_aer.noise import NoiseModel

noise_model = NoiseModel()
noise_model.add_quantum_error(coherent_error.compose(incoherent_error), "x", (0,))
noise_model.add_readout_error(readout_error, (0,))
```



#### 2.3 노이즈 모델을 사용하여 노이즈가 있는 샘플러를 구축하세요.

```python
from qiskit_aer.primitives import SamplerV2 as Sampler

noisy_sampler = Sampler(options={"backend_options": {"noise_model": noise_model}})
```


#### 2.4 잡음이 있는 샘플러에서 양자 회로를 실행하세요

```python
job = noisy_sampler.run(circuits, shots=400)
```

```python
result = job.result()
```

```python
result[0].data.meas.get_counts()
```

```
{'0': 389, '1': 11}
```


#### 2.5 그래프 결과

```python
import matplotlib.pyplot as plt

plt.title("Noisy simulation")
ds = list(range(MAX_DEPTH + 1))
plt.plot(
    ds,
    [result[d].data.meas.expectation_values(["Z"]) for d in ds],
    color="gray",
    linestyle="-",
)
plt.scatter(ds, [result[d].data.meas.expectation_values(["Z"]) for d in ds], marker="o")
plt.hlines(0, xmin=0, xmax=MAX_DEPTH, colors="black")
plt.ylim(-1, 1)
plt.xlabel("Circuit depth")
plt.ylabel("Measured <Z>")
plt.show()
```


#### 2.6 이상적인 시뮬레이션

```python
ideal_sampler = Sampler()
job_ideal = ideal_sampler.run(circuits)
result_ideal = job_ideal.result()
plt.title("Ideal simulation")
ds = list(range(MAX_DEPTH + 1))
plt.plot(
    ds,
    [result_ideal[d].data.meas.expectation_values(["Z"]) for d in ds],
    color="gray",
    linestyle="-",
)
plt.scatter(
    ds, [result_ideal[d].data.meas.expectation_values(["Z"]) for d in ds], marker="o"
)
plt.hlines(0, xmin=0, xmax=MAX_DEPTH, colors="black")
plt.xlabel("Circuit depth")
plt.ylabel("Measured <Z>")
plt.show()
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Ferror-mitigation%2Fextracted-outputs%2F041abc81-0.avif&w=1200&q=75)


#### 2.7 연습
아래 코드를 수정하여 다음을 수행하십시오.

* [ ] 샷 수를 25배(= 10,000회)로 늘려 그래프가 더 부드러워지는지 확인하십시오.
* [ ] 노이즈 매개변수(OVER_ROTATION_ANGLE, AMPLITUDE_DAMPING_PARAM, PREP0_MEAS1 또는 PREP1_MEAS0)를 변경하고 그래프가 어떻게 변하는지 확인하십시오.

```python
OVER_ROTATION_ANGLE = 0.05
coherent_error = coherent_unitary_error(RXGate(OVER_ROTATION_ANGLE).to_matrix())
AMPLITUDE_DAMPING_PARAM = 0.02  # in [0, 1] (0: no error)
incoherent_error = amplitude_damping_error(AMPLITUDE_DAMPING_PARAM)
PREP0_MEAS1 = 0.1  # P(1|0): Probability of preparing 0 and measuring 1
PREP1_MEAS0 = 0.05  # P(0|1): Probability of preparing 1 and measuring 0
readout_error = ReadoutError(
    [[1 - PREP0_MEAS1, PREP0_MEAS1], [PREP1_MEAS0, 1 - PREP1_MEAS0]]
)
noise_model = NoiseModel()
noise_model.add_quantum_error(coherent_error.compose(incoherent_error), "x", (0,))
noise_model.add_readout_error(readout_error, (0,))
options = {
    "backend_options": {"noise_model": noise_model},
}
noisy_sampler = Sampler(options=options)
job = noisy_sampler.run(circuits, shots=400)
result = job.result()
plt.title("Noisy simulation")
ds = list(range(MAX_DEPTH + 1))
plt.plot(
    ds,
    [result[d].data.meas.expectation_values(["Z"]) for d in ds],
    marker="o",
    linestyle="-",
)
plt.hlines(0, xmin=0, xmax=MAX_DEPTH, colors="black")
plt.ylim(-1, 1)
plt.xlabel("Depth")
plt.ylabel("Measured <Z>")
plt.show()
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Ferror-mitigation%2Fextracted-outputs%2F502b9cfe-0.avif&w=1200&q=75)


#### 2.8 보다 현실적인 노이즈 시뮬레이션

```python
from qiskit_aer import AerSimulator
from qiskit_ibm_runtime import SamplerV2 as Sampler, QiskitRuntimeService

service = QiskitRuntimeService()
real_backend = service.least_busy(
    operational=True, simulator=False, min_num_qubits=127
)  # Eagle
```

```python
aer = AerSimulator.from_backend(real_backend)
noisy_sampler = Sampler(mode=aer)
job = noisy_sampler.run(circuits)
result = job.result()
plt.title("Noisy simulation with noise model from real backend")
ds = list(range(MAX_DEPTH + 1))
plt.plot(
    ds,
    [result[d].data.meas.expectation_values(["Z"]) for d in ds],
    marker="o",
    linestyle="-",
)
plt.hlines(0, xmin=0, xmax=MAX_DEPTH, colors="black")
plt.ylim(-1, 1)
plt.xlabel("Depth")
plt.ylabel("Measured <Z>")
plt.show()
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Ferror-mitigation%2Fextracted-outputs%2F81a67f2f-0.avif&w=1200&q=75)


## 3. 오류 완화 기능을 갖춘 실제 양자 컴퓨팅

이 부분에서는 Qiskit Estimator를 사용하여 오류가 완화된 결과(기대값)를 얻는 방법을 보여줍니다.

6큐비트 트로터화 회로를 사용하여 1차원 이징 모델의 시간 진화를 시뮬레이션하고, 시간 단계 수에 따라 오류가 어떻게 증가하는지 살펴봅니다.

```python
backend = service.least_busy(
    operational=True, simulator=False, min_num_qubits=127
)  # Eagle
backend
```

```python
NUM_QUBITS = 6
NUM_TIME_STEPS = list(range(8))
RX_ANGLE = 0.1
RZZ_ANGLE = 0.1
```


#### 3.1 회로를 구성하세요

```python
# Build circuits with different number of time steps
circuits = []
for n_steps in NUM_TIME_STEPS:
    circ = QuantumCircuit(NUM_QUBITS)
    for i in range(n_steps):
        # rx layer
        for q in range(NUM_QUBITS):
            circ.rx(RX_ANGLE, q)
        # 1st rzz layer
        for q in range(1, NUM_QUBITS - 1, 2):
            circ.rzz(RZZ_ANGLE, q, q + 1)
        # 2nd rzz layer
        for q in range(0, NUM_QUBITS - 1, 2):
            circ.rzz(RZZ_ANGLE, q, q + 1)
    circ.barrier()  # need not to optimize the circuit
    # Uncompute stage
    for i in range(n_steps):
        for q in range(0, NUM_QUBITS - 1, 2):
            circ.rzz(-RZZ_ANGLE, q, q + 1)
        for q in range(1, NUM_QUBITS - 1, 2):
            circ.rzz(-RZZ_ANGLE, q, q + 1)
        for q in range(NUM_QUBITS):
            circ.rx(-RX_ANGLE, q)
    circuits.append(circ)
```

이상적인 출력을 미리 알기 위해, 우리는 원래 회로 $U$가 적용되는 첫 번째 단계와 이를 반전시킨 $U^\dagger$가 적용되는 두 번째 단계로 구성된 계산-역계산 회로를 사용합니다.
이러한 회로의 이상적인 결과는 입력 상태 $\|000000\rangle$이 될 것이며, 이는 임의의 파울리 관측량에 대해 자명한 기대값을 갖습니다. 예를 들어, $\langle IIIIIIZ \rangle = 1$입니다.

```python
# Print the circuit with 2 time steps
circuits[2].draw(output="mpl")
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Ferror-mitigation%2Fextracted-outputs%2F20296b5a-0.avif&w=3840&q=75)

참고: 위 그림과 같이 $k$개의 시간 단계를 갖는 회로는 $4k$개의 2큐비트 게이트 레이어를 갖게 됩니다.

```python
obs = SparsePauliOp.from_sparse_list([("Z", [0], 1.0)], num_qubits=NUM_QUBITS)
obs
```

```
SparsePauliOp(['IIIIIZ'],
              coeffs=[1.+0.j])
```


#### 3.2 회로를 트랜스파일링합니다.

백엔드용 회로를 최적화(optimization_level=1)를 적용하여 트랜스파일링합니다.

```python
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager

pm = generate_preset_pass_manager(optimization_level=1, backend=backend)
isa_circuits = pm.run(circuits)
display(isa_circuits[2].draw("mpl", idle_wires=False, fold=-1))
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Ferror-mitigation%2Fextracted-outputs%2F87b861e2-0.avif&w=3840&q=75)


#### 3.3 Estimator를 사용한 실행 (다양한 복원력 수준 지원)

Qiskit Estimator를 사용할 때 오류 완화를 적용하는 가장 쉬운 방법은 복원력 수준(`estimator.options.resilience_level`)을 설정하는 것입니다. Estimator는 다음 복원력 수준을 지원합니다(2024년 6월 28일 기준). 자세한 내용은 [오류 완화 구성](/docs/guides/error-mitigation-and-suppression-techniques) 가이드를 참조하십시오.

![image.png](https://quantum.cloud.ibm.com/learning/images/courses/utility-scale-quantum-computing/error-mitigation/res_level.avif)


```python
from qiskit_ibm_runtime import Batch
from qiskit_ibm_runtime import EstimatorV2 as Estimator

jobs = []
job_ids = []
with Batch(backend=backend):
    for resilience_level in [0, 1, 2]:
        estimator = Estimator()
        estimator.options.resilience_level = resilience_level
        job = estimator.run(
            [(circ, obs.apply_layout(circ.layout)) for circ in isa_circuits]
        )
        job_ids.append(job.job_id())
        print(f"Job ID (rl={resilience_level}): {job.job_id()}")
        jobs.append(job)
```

```
Job ID (rl=0): d146vcnmya70008emprg
Job ID (rl=1): d146vdnqf56g0081sva0
Job ID (rl=2): d146ven5z6q00087c61g
```

```python
# check job status
for job in jobs:
    print(job.status())
```

```
DONE
DONE
DONE
```

```python
# REPLACE WITH YOUR OWN JOB IDS
jobs = [service.job(job_id) for job_id in job_ids]
```

```python
# Get results
results = [job.result() for job in jobs]
```


#### 3.4 그래프 결과

```python
plt.title("Error mitigation with different resilience levels")
labels = ["0 (No mitigation)", "1 (TREX)", "2 (ZNE + Gate twirling)"]
steps = NUM_TIME_STEPS
for result, label in zip(results, labels):
    plt.errorbar(
        x=steps,
        y=[result[s].data.evs for s in steps],
        yerr=[result[s].data.stds for s in steps],
        marker="o",
        linestyle="-",
        capsize=4,
        label=label,
    )
plt.hlines(
    1.0, min(steps), max(steps), linestyle="dashed", label="Ideal", colors="black"
)
plt.xlabel("Time steps")
plt.ylabel("Mitigated <IIIIIZ>")
plt.legend()
plt.show()
```

![](https://quantum.cloud.ibm.com/assets-docs-learning/_next/image?url=%2Flearning%2Fimages%2Fcourses%2Futility-scale-quantum-computing%2Ferror-mitigation%2Fextracted-outputs%2F7527976e-0.avif&w=1200&q=75)


## 4. (선택 사항) 오류 완화 옵션을 사용자 지정합니다.

아래와 같이 옵션을 통해 오류 완화 기법의 적용 방식을 맞춤 설정할 수 있습니다.

```python
# TREX
estimator.options.twirling.enable_measure = True
estimator.options.twirling.num_randomizations = "auto"
estimator.options.twirling.shots_per_randomization = "auto"

# Gate twirling
estimator.options.twirling.enable_gates = True
# ZNE
estimator.options.resilience.zne_mitigation = True
estimator.options.resilience.zne.noise_factors = [1, 3, 5]
estimator.options.resilience.zne.extrapolator = ("exponential", "linear")

# Dynamical decoupling
estimator.options.dynamical_decoupling.enable = True  # Default: False
estimator.options.dynamical_decoupling.sequence_type = "XX"

# Other options
estimator.options.default_shots = 10_000
```

오류 완화 옵션에 대한 자세한 내용은 다음 가이드 및 API 참조를 확인하세요.

* [오류 완화 구성](https://quantum.cloud.ibm.com/docs/guides/error-mitigation-and-suppression-techniques)
* [옵션 소개](https://quantum.cloud.ibm.com/docs/guides/runtime-options-overview)
* [추정기 옵션](https://quantum.cloud.ibm.com/docs/api/qiskit-ibm-runtime/options-estimator-options)
* [샘플러 옵션](https://quantum.cloud.ibm.com/docs/api/qiskit-ibm-runtime/options-sampler-options)