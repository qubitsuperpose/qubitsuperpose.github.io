---
title: 13차시 9:Qiskit2.0 Certificate Exam Tutorial 9
layout: single
classes: wide
categories:
  - Qiskit Certificate
toc: true # 이 포스트에서 목차를 활성화
toc_sticky: true # 목차를 고정할지 여부 (선택 사항)
---

![](https://avatars.githubusercontent.com/u/41856731?s=96&v=4)
[출처:https://github.com/kibrahim757/qiskit_2x_certification_exam_tutorial](https://github.com/kibrahim757/qiskit_2x_certification_exam_tutorial/tree/main/Section_8_Operate_WITH_OpenQASM)

# Practice Test

---

The exam consists of 68 questions, following the format described on the IBM certification page. The assessment covers the following sections:

**Exam Sections:**

- Perform quantum operations (16% - ~11 questions)

- Visualize quantum circuits, measurements, and states (11% - ~7 questions)

- Create quantum circuits (18% - ~12 questions)

- Run quantum circuits (15% - ~10 questions)

- Use the sampler primitive (12% - ~8 questions)

- Use the estimator primitive (12% - ~8 questions)

- Retrieve and analyze the results of quantum circuits (10% - ~7 questions)

- Operate with OpenQASM (6% - ~5 questions)

You can find more information about the exam here:

https://www.ibm.com/training/certification/ibm-certified-quantum-computation-using-qiskit-v2x-developer-associate-C9008400

----

**Question 1**

**Given the boolean array representation, which Pauli operator is created?**

```
from qiskit.quantum_info import Pauli
import numpy as np

z = np.array([True, False, False])  
x = np.array([False, True, False])
p = Pauli((z, x))
print(p)
```

A) IXY

B) IXZ

C) XZI with an imaginary phase

D) YXI


<Details><summary>정답:</summary>

Correct Answer: B <br/>

The result is <br>

    Position 0: (z=True, x=False) → Z <br>

    Position 1: (z=False, x=True) → X <br>

    Position 2: (z=False, x=False) → I <br>

    Since Qiskit displays in little-endian (rightmost is qubit 0), this gives IXZ.
</Details>

---

**Question 2**

**What happens when you try to create a Pauli operator from this circuit?**

```
from qiskit import QuantumCircuit
from qiskit.quantum_info import Pauli

qc = QuantumCircuit(3)
qc.x(0)
qc.z(1)  
qc.h(2)
p = Pauli(qc)
```

A) Creates XZH

B) Creates HZX

C) Returns an error

D) Creates XZH but with a warning


<Details><summary>정답:</summary>

Correct Answer: C
<br/>
Pauli operators can only be extracted from circuits containing Pauli gates (I, X, Y, Z). The Hadamard gate (H) is not a Pauli gate, so this will raise an error.

</Details>



---

**Question 3**

**Which statement is TRUE on the code below?**

```
qc = QuantumCircuit(1)
qc.x(0)
qc.h(0)
```
Compared To
```
qc = QuantumCircuit(1)
qc.h(0)
qc.x(0)
```

A) Both circuits produce the same final state

B) The circuits differ only by a global phase

C) The circuits differ by a relative phase

D) Both are equivalent up to the measurement


<Details><summary>정답:</summary>

Correct Answer: C
<br/>
The result of the first circuit is ($|0\rangle$-$|1\rangle$)/√2 while the second one will be ($|0\rangle$+$|1\rangle$)/√2 , they differ by a relative phase 

</Details>



---

**Question 4**

**Which circuit is functionally equivalent to a CZ gate?**

A)
```
qc.cx(0, 1)
```

B)
```
qc.h(1)
qc.cx(0, 1)
qc.h(1)
```

C)
```
qc.x(1)
qc.cx(0, 1)
```

D)
```
qc.z(1)
qc.cx(0, 1)
```


<Details><summary>정답:</summary>

Correct Answer: B
<br/>
A Hadamard transform on the target qubit converts CX into CZ:

CZ=(I⊗H) CX (I⊗H)

</Details>


---

**Question 5**

**When using Qiskit's circuit library, what is the main practical difference between using a component's `Gate` representation versus its `QuantumCircuit` representation ?**

A) Gate representations are faster to execute on hardware, while QuantumCircuit representations are better for simulation.

B) Gate representations are abstract mathematical operations optimized by the transpiler, while QuantumCircuit representations are concrete implementations.

C) Gate representations have more parameters, while QuantumCircuit representations are fixed.

D) QuantumCircuit representations only work in simulation, while Gate representations work on real hardware.

E) There is no functional difference; they can be used interchangeably in all cases.


<Details><summary>정답:</summary>

Correct Answer: B
<br/>
Gate representations are abstract mathematical operations optimized by the transpiler, while QuantumCircuit representations are concrete implementations.
</Details>

---

**Question 6**

**Which of the following is a correct description of a template circuit used in transpiler optimizations?**

A) A set of quantum gates used for encoding classical data

B) A configuration file that describes backend coupling maps

C) A minimal rewrite rule circuit, where a pattern can be replaced with a shorter equivalent pattern

D) A circuit specifically designed to generate GHZ states


<Details><summary>정답:</summary>

Correct Answer: C
<br/>
Template circuits provide pattern-matching rules so the transpiler can replace inefficient gate sequences with optimized ones.

</Details>

---

**Question 7**

**Which gate makes a circuit non-Clifford circuit?**

A) H

B) S

C) CX

D) T


<Details><summary>정답:</summary>

Correct Answer: D
<br/>
H, S, and CX are Clifford gates, T is a non-Clifford gate
</Details>

---

**Question 8**

**What does the circuit below do?**

```
from qiskit.circuit.library import IntegerComparator

qc = IntegerComparator(num_state_qubits=3, value=5)
```

A) Adds 5 to a 3-qubit register

B) Compares a 3-qubit integer to 5 and returns true if  equal

C) Outputs 1 if the input integer ≥ 5

D) Outputs 1 if the input integer ≤ 5


<Details><summary>정답:</summary>

Correct Answer: C
<br/>
IntegerComparator sets the output qubit to 1 when the encoded integer is ≥ value.
</Details>

---

**Question 9**

**What is the key difference between Z Feature Map and ZZ Feature Map?**

A) ZFM uses only single-qubit gates, ZZFM adds two-qubit entangling gates

B) ZFM encodes features in rotation angles, ZZFM encodes in amplitudes

C) ZFM requires more qubits than features, ZZFM requires fewer

D) ZFM is classical, ZZFM is quantum


<Details><summary>정답:</summary>

Correct Answer: A
<br/>
ZFM consists of Hadamard and phase gates (single-qubit), while ZZFM extends this with two-qubit entangling gates (RZZ rotations).
</Details>

---

**Question 10**

**Consider these two circuits:**

```
qc1 = QuantumCircuit(1); qc1.h(0)
qc2 = QuantumCircuit(1); qc2.x(0)
```

If you want to combine qc2 into qc1 such that the X gate appears inline in the final circuit (not as a boxed subcircuit), which method should you use?

A) ```qc1.add(qc2.to_gate(), [0])```

B) ```qc1.tensor(qc2)```

C) ```qc1.compose(qc2, [0])```

D) ```qc1.add_register(qc2.qregs[0])```




<Details><summary>정답:</summary>
<br/>
Correct Answer: C
<br/>
compose applies the instructions from one circuit onto specified qubits and/or clbits on another.
</Details>


---

**Question 11**

**When using reverse_bits() on a 3-qubit circuit, What happens to the gates in qc_rev?**

```
qc = QuantumCircuit(3)
qc.x(0)
qc.z(2)
qc_rev = qc.reverse_bits()
```


A) The X gate moves to qubit 2 and the Z gate moves to qubit 0

B) Both gates remain on their original qubits but are displayed in reverse order

C) The circuit becomes equivalent to qc.inverse()

D) All gates are converted to their controlled versions


<Details><summary>정답:</summary>
<br/>
Correct Answer: A
<br/>
reverse_bits reverses the order of the bits making qubit 0 at the bottom and qubit 2 at the top of the circuit.
</Details>

---

**Question 12**

**Which visualization function would be most appropriate for showing a single qubit's superposition state on the Bloch sphere?**

A) plot_state_qsphere() 

B) plot_bloch_vector($\[1/√2, 1/√2, 0\]$)

C) plot_bloch_multivector(state) 

D) plot_state_city(state)


<Details><summary>정답:</summary>
<br/>
Correct Answer: B
<br/>
plot_bloch_vector is typically used for plotting single qubit states using Cartesian or spherical coordinates on the Bloch sphere.
</Details>

---

**Question 13**

**For the code below, whcih statement is correct?**

```
from qiskit import QuantumCircuit
 
circuit = QuantumCircuit(6,6)
circuit.h(0)
circuit.cx(0,1)
circuit.cx(0, 2) 
circuit.cx(1, 3) 
circuit.cx(0, 4)
circuit.cx(1, 5)

circuit.measure_all()

circuit.draw(output="mpl", fold=3)
```

A) Fold changes execution order

B) Fold compresses time into layers

C) Fold only affects visual line wrapping

D) Vertical alignment implies concurrency


<Details><summary>정답:</summary>
<br/>
Correct Answer: C
<br/>
fold only changes layout, not logical or physical parallelism.

</Details>

---

**Question 14**

**You have created a 3-qubit circuit and want to visualize each qubit's state separately. Which function should you use?**

```
from qiskit import QuantumCircuit
from qiskit.quantum_info import Statevector
qc = QuantumCircuit(3)
qc.x(0)
qc.h(1)
qc.y(2)
state = Statevector(qc)
```

A) plot_bloch_vector(state, num_qubits=3)

B) plot_state_qsphere(state)

C) plot_state_hinton(state)

D) plot_bloch_multivector(state)



<Details><summary>정답:</summary>
<br/>
Correct Answer: D
<br/>
plot_bloch_multivector creates separate Bloch spheres for each qubit in a multi-qubit state.

</Details>

---

**Question 15**

**Which visualization function would be most appropriate for analyzing how a quantum circuit is mapped to physical hardware after transpilation?**

A) plot_gate_map(backend)

B) plot_circuit_layout(circuit, backend)

C) plot_coupling_map(num_qubits, coupling_map)

D) plot_error_map(backend)


<Details><summary>정답:</summary>
<br/>

Correct Answer: B
<br/>
plot_circuit_layout can be used to plot circuit layout for a given target backend .

</Details>

---

**Question 16**

**For the code below, which statement is correct?**

```
qc = QuantumCircuit(2)
qc.measure(0, 0)
qc.draw(output="mpl")
```

A) Measurement requires circuit transpilation

B) Classical bits must be explicitly defined when ```measure```  is used

C) Measurement is only supported in text mode

D) The measurement syntax is incorrect


<Details><summary>정답:</summary>
<br/>
Correct Answer: B
<br/>
Classical Bits must be created before calling measure

```qc = QuantumCircuit(2, 1)```

</Details>

---

**Question 17**

**Which of the following diagrams BEST represents the output of this code?**

```
from qiskit import QuantumCircuit
from qiskit.quantum_info import Statevector
from qiskit.visualization import plot_bloch_multivector,plot_state_qsphere

qc = QuantumCircuit(2)
qc.h(0)
qc.cx(0,1)

state = Statevector(qc)
plot_state_qsphere(state)
```

A) <Image src="/assets/images/sample_test01.png" style="width: 200px; height: auto;"/>

B) <Image src="/assets/images/sample_test02.png" style="width: 200px; height: auto;"/>

C) <Image src="/assets/images/sample_test03.png" style="width: 400px; height: auto;"/>

D) <Image src="/assets/images/sample_test04.png" style="width: 400px; height: auto;"/>





<Details><summary>정답:</summary>
<br/>
Correct Answer: A
<br/>
The output is Bell state ($|Φ+\rangle$ = ($|00\rangle$+$|11\rangle$)/√2 , qsphere plot will show 00 and 11 with phase 0
</Details>

---

**Question 18**

**Which visualization function would be most appropriate to view the real and imaginary parts of a quantum state ?**

A) plot_distribution

B) plot_histogram

C) plot_state_city

D) plot_city_state


<Details><summary>정답:</summary>
<br/>
Correct Answer: C
<br/>
plot_state_city plots two 3d bar graphs (two dimensional) of the real and imaginary part of the density matrix
</Details>

---

**Question 19**

**Consider the following code:**

```
from qiskit.circuit import QuantumCircuit, QuantumRegister, ClassicalRegister

qubits = QuantumRegister(2)
clbits = ClassicalRegister(2)
circuit = QuantumCircuit(qubits, clbits)
(q0,q1) = qubits
(c0,c1) = clbits

circuit.h(q0)
circuit.measure(q0, c0)

with circuit.if_test((c0, 1)) as else_:
    circuit.x(q1)
with else_:
    circuit.h(q1)

circuit.measure(q1, c1)
```

If the first measurement result c0 is 0, what will be the measurement probabilities for c1?

A) 100% probability of c1=0

B) 100% probability of c1=1

C) 50% probability of c1=0 and 50% of c1=1

D) 25% probability of c1=0 and 75% of c1=1


<Details><summary>정답:</summary>
<br/>
Correct Answer: C
<br/>
When c0=0, the else block executes, applying an H gate to q1, creating a superposition state with equal probabilities for 0 and 1.
</Details>

---

**Question 20**

**Which of the following is a not a limitation when using dynamic circuits in Qiskit Runtime?**

A) Nested if_test statements

B) Using 32 bits or less  in classical registers in if_test

C) Arithmetic operations within classical expressions

D) For and while loops inside conditionals


<Details><summary>정답:</summary>
<br/>
Correct Answer: B
<br/>
Using less than 32 bits in classical register is allowed while using dynamic circuits
</Details>

---

**Question 21**

**You want to execute a dynamic circuit with classical feedforward on real quantum hardware. Which approach would correctly identify backends that support this feature?**

A)
```
from qiskit_ibm_runtime import QiskitRuntimeService
service = QiskitRuntimeService()
backends = service.backends(control_flow=True)
```
B)
```
from qiskit_ibm_runtime import QiskitRuntimeService
service = QiskitRuntimeService()
backends = service.backends(dynamic_circuits=True)
```
C)
```
from qiskit import IBMQ
provider = IBMQ.load_account()
backends = provider.backends(supports_feedforward=True)
```
D)
```
from qiskit import Aer
backends = Aer.backends(control_flow=True)
```


<Details><summary>정답:</summary>
<br/>
Correct Answer: B
<br/>
service.backends(dynamic_circuits=True) is the correct way to find backends supporting dynamic circuits.
</Details>

---

**Question 22**

**Why will the code below fail?**

```
with qc.if_test((qc.clbits[0] + qc.clbits[1], 1)):
    qc.x(0)

```


A) Arithmetic operations is not supported in conditionals

B) Boolean OR operation(+)  is not supported in conditionals

C) Multi-bit conditions are not allowed

D) Multiple classical bits require registers


<Details><summary>정답:</summary>
<br/>
Correct Answer: A
<br/>
Conditional expressions must be simple comparisons, no arithmetic operators.
</Details>

---

**Question 23**

**You have a circuit with two parameters $\theta$ and $\gamma$. Which method correctly binds both parameters to specific values?**

```
theta = Parameter('θ')
gamma = Parameter('γ')
qc = QuantumCircuit(2)
qc.ry(theta, 0)
qc.rz(gamma, 1)
```

A) qc.bind(theta=np.pi/2, gamma=np.pi/4)

B) qc.assign_parameters($\[np.pi/2, np.pi/4\]$)

C) qc.assign_parameters({theta: np.pi/2, gamma: np.pi/4})

D) qc.set_parameters(theta=np.pi/2, gamma=np.pi/4)


<Details><summary>정답:</summary>
<br/>
Correct Answer: C
<br/>
you can use assign_parameters({theta: np.pi/2, gamma: np.pi/4}) to bind multiple parameters to values.

</Details>

---

**Question 24**

**What is the output of the code below?**

```
from qiskit.circuit import Parameter
import numpy as np

x = Parameter('x')
y = Parameter('y')
expr = 2 * x - y**2
print(f"Expression parameters: {expr.parameters}")
```

A) {x}

B) {y}

C) {Parameter(x), Parameter(y)}

D) {} (empty set)

E) An error because expressions don't have a parameters attribute


<Details><summary>정답:</summary>
<br/>
Correct Answer: C
<br/>
expr.parameters returns all parameters in the expression.

</Details>

---

**Question 25**

**How many distinct parameters does the circuit below contain?**

```
alpha = Parameter('α')
beta = Parameter('β')
expr = alpha - 2 * beta

qc = QuantumCircuit(1)
qc.rx(alpha + beta, 0)
qc.ry(expr, 0)
print(f"All parameters: {qc.parameters}")
```

A) 1 parameter

B) 2 parameters

C) 3 parameters

D) 4 parameters


<Details><summary>정답:</summary>
<br/>
Correct Answer: B
<br/>
The circuit uses expressions alpha + beta and 2*alpha - beta, but both expressions are built from the same base parameters alpha and beta, so qc.parameters returns {Parameter(α), Parameter(β)}.
</Details>

---

**Question 26**

**What is the correct way to evaluate a parameter expression after binding values?**

```
x = Parameter('x')
y = Parameter('y')
expr = (x + y) * np.cos(x) - y**2
param_values = {x: 1.0, y: 2.0}
```

A) result = expr.evaluate(param_values)

B) result = expr.bind(param_values)

C) result = expr.substitute(param_values).calculate()

D) result = expr.with_values(param_values).eval()


<Details><summary>정답:</summary>
<br/>
Correct Answer: B
<br/>
bind() method is used to assign the parameter values to the expressions.

</Details>

---

**Question 27**

**Which layout strategy would be most appropriate for a circuit that needs to be mapped to hardware qubits with a simple sequential mapping (qubit 0→0, 1→1, etc.)?**

```

from qiskit.transpiler import generate_preset_pass_manager
pass_manager = generate_preset_pass_manager(
    optimization_level=1,
    backend=backend,
    layout_method=???
)

```

A) layout_method="vf2"

B) layout_method="dense"

C) layout_method="sabre"

D) layout_method="trivial"


<Details><summary>정답:</summary>
<br/>
Correct Answer: D
<br/>
"TrivialLayout: Simple sequential mapping (qubit 0→0, 1→1, etc.)"
</Details>

---

**Question 28**

**What is the primary purpose of the translation stage in Qiskit's transpiler?**

A) To schedule gates for optimal timing on hardware

B) To map virtual qubits to physical qubits

C) To convert all gates to the hardware's native basis gates

D) To insert SWAP gates for connectivity constraints


<Details><summary>정답:</summary>
<br/>
Correct Answer: C
<br/>
All gates in the circuit are converted to the hardware's native basis gates in the translation stage.
</Details>

---

**Question 29**

**What is the correct order of Qiskit's transpiler stages?**

A) Initialization → Layout → Routing → Transpilation → Optimization → Scheduling

B) Initialization → Routing → Layout → Transpilation → Scheduling

C) Initialization → Layout → Routing → Translation → Optimization → Scheduling

D) Initialization → Layout → Optimization → Translation → Execution


<Details><summary>정답:</summary>
<br/>
Correct Answer: C
<br/>
The stages are: Initialization → Layout → Routing → Translation → Optimization → Scheduling, however routing and layout are typically computed together.
</Details>

---

**Question 30**

**When creating a custom gate by subclassing Gate, which method must be overridden to provide the circuit implementation?**

```
class MyCustomGate(Gate):
    def __init__(self):
        super().__init__("my_gate", 2, [])
    
    def ???(self):
        defn = QuantumCircuit(2)
        defn.h(0)
        defn.cx(0, 1)
        self._definition = defn
```

A) _define()

B) _build()

C) _create()

D) _implement()


<Details><summary>정답:</summary>
<br/>
Correct Answer: A
_define() method should be overridden to provide the circuit implementation for the instruction or gate.
<br/>
</Details>

---

**Question 31**

**Which function is used to apply Pauli twirling specifically to two-qubit gates in a circuit?**

```
from qiskit.circuit import ???

twirled_circuit = ???(qc, seed=42)
```

A) pauli_twirl_2q_gates()

B) apply_pauli_twirl()

C) randomize_gates()

D) twirl_two_qubit()


<Details><summary>정답:</summary>
<br/>
Correct Answer: A
<br/>
pauli_twirl_2q_gates can be used to apply pauli twirling around specific two qubit gates
</Details>

---

**Question 32**

**Which statement about batch execution mode is correct?**


A) Jobs are executed sequentially on the QPU

B) The batch remains open until manually closed

C) All jobs in a batch are scheduled together

D) Batch mode guarantees exclusive QPU access


<Details><summary>정답:</summary>
<br/>
Correct Answer: C
<br/>
In batch mode, all jobs are scheduled together, reducing queue overhead, but exclusive access is not guaranteed.

</Details>

---

**Question 33**

**What happens when the maximum TTL is reached?**

```
batch = Batch(backend=backend, max_time=300)
estimator = Estimator(batch=batch)
estimator.run(circuits=[qc])

```

A) Only new jobs are rejected

B) All running jobs finish normally

C) Queued jobs fail, and running jobs are stopped

D) The batch automatically restarts


<Details><summary>정답:</summary>
<br/>
Correct Answer: B
<br/>
When the maximum TTL is reached, Any job running is allowed to finish, but all queued jobs are failed
</Details>

---

**Question 34**

**When is Session mode preferred over Batch mode?**

A) When running a large set of independent circuits

B) When you have one complex circuit

C) When you want the cheapest execution option

D) When executing variational algorithms that require iterative classical-quantum communication


<Details><summary>정답:</summary>
<br/>
Correct Answer: D
<br/>
Session mode is best for iterative workflows or when dedicated QPU access is required.

</Details>

---

**Question 35**

**Consider the following code snippet. What will happen when job3 is submitted?**

```
from qiskit_ibm_runtime import Batch, SamplerV2 as Sampler

batch = Batch(backend=backend, max_time="10m")
sampler = Sampler(mode=batch)

job1 = sampler.run([circuit1])
job2 = sampler.run([circuit2])

batch.close()

job3 = sampler.run([circuit3])
```

A) job3 will fail because the batch is closed and no longer accepts jobs.

B) job3 will be added to the batch and run normally.

C) The batch will automatically reopen for job3.

D) The code will return an error, because max TTL is 5 minutes.


<Details><summary>정답:</summary>
<br/>
Correct Answer: A
<br/>
Once the batch is closed, new jobs are not accepted.

</Details>

---

**Question 36**

**Consider the following code:**

```
from qiskit_ibm_runtime import Batch, Estimator

batch = Batch(backend=backend,max_time=600)

estimator = Estimator(batch=batch)

# Job A
estimator.run(circuits=[qc1], observables=[obs1])

# Job B is submitted 3 minutes after Job A
estimator.run(circuits=[qc2], observables=[obs2])
```

What happens when Job B is submitted?

A) Job B is rejected because the batch is permanently closed

B) Job B starts a new batch automatically

C) The batch is deactivated after 1 minute, and then Job B reactivates the batch and runs normally

D) Job B is queued but will not be executed


<Details><summary>정답:</summary>
<br/>
Correct Answer: C
<br/>
If there is no jobs on the queue for 1 minute (Interactive TTL), the batch is temporarily deactivated, when job B is triggered, the batch is activated and job B will run 
</Details>

---

**Question 37**

**What is the primary purpose of the generate_preset_pass_manager() function when executing circuits on real hardware?**

A) To authenticate with IBM Quantum Runtime service

B) To transpile circuits for specific hardware constraints like native gates and qubit connectivity

C) To select the least busy backend automatically

D) To calculate expectation values for observables


<Details><summary>정답:</summary>
<br/>
Correct Answer: B
<br/>
The pass manager adapts circuits to the backend's native gate set, qubit connectivity, and optimization level.
</Details>

---

**Question 38**

**When using broadcasting with the Estimator primitive, what is the output shape for the following input shapes?**

```
# Parameter values shape: (1 × 6)   [1 parameter set with 6 values]
# Observables shape:      (4 × 1)   [4 observables with 1 each]
# Output shape:          ?
```

A) (1 × 6)

B) (4 × 1)

C) (6 × 4)

D) (4 × 6)


<Details><summary>정답:</summary>
<br/>
Correct Answer: D
<br/>
This is an outer product broadcast where each of the 4 observables is computed for each of the 6 parameter values.

</Details>

---

**Question 39**

**Which of the following statements about Sessions is CORRECT? $\[Choose Two\]$**

A) Sessions are available to all IBM Quantum users, including Open Plan users

B) Sessions provide dedicated QPU access and maintain context between jobs for iterative algorithms

C) Sessions are more cost-effective than Batch mode for running independent circuits

D) Sessions have a fixed maximum duration of 10 minutes for all account types

E) Sessions should be used be default

F) Sessions are more expensive than Batch mode


<Details><summary>정답:</summary>
<br/>
Correct Answers: B,F

</Details>

---

**Question 40**

**Which execution mode would be MOST appropriate for running 50 independent circuits where cost efficiency is the priority?**

A) Batch mode

B) Session mode

C) Job mode

D) On a simulator


<Details><summary>정답:</summary>
<br/>
Correct Answer: A
<br/>
Batch mode is ideal for running independent jobs, and it less expensive than session mode

</Details>

---

**Question 41**

**Which of the following correctly configures the Sampler to use a default of 500 shots?**

A)
```
sampler = Sampler(backend=backend)
sampler.default_shots = 500
```
B)
```
sampler = Sampler(backend=backend, shots=500)
```

C)
```
sampler = Sampler(backend=backend)
sampler.options.shots = 500
```

D)
```
sampler = Sampler(backend=backend, options={"default_shots": 500})
```


<Details><summary>정답:</summary>
<br/>
Correct Answer: D
<br/>
The `default_shots` option is set through the `options` dictionary during initialization.

</Details>

---

**Question 42**

**What is wrong with this sampler.run() call below?**

```
from qiskit import QuantumCircuit
from qiskit.primitives import BackendSamplerV2 as Sampler
from qiskit_aer import AerSimulator

backend = AerSimulator()
sampler = Sampler(backend=backend)

qc = QuantumCircuit(3)
qc.h(0)
qc.cx(0, 1)
qc.cx(1, 2)
qc.measure_all()


job = sampler.run([(qc),[0.5, 0.2]])
```

A) The run() method expects a list of circuits, not a single circuit

B) The number of shots are not sepcified

C) The circuit needs to be transpiled before passing to run()

D) The circuit doesn't have parameters, so the parameter values $[0.5, 0.2]$ shouldn't be included 



<Details><summary>정답:</summary>
<br/>
Correct Answer: D
<br/>
qc has no parameters (no Parameter objects), the second element $[0.5, 0.2]$ in the tuple is invalid. 
</Details>

---

**Question 43**

**What is the main difference between Pauli Twirling and Dynamical Decoupling in terms of what they mitigate?**

A) Twirling mitigates idle time errors, Dynamical Decoupling mitigates gate errors

B) Twirling converts coherent errors to incoherent noise, Dynamical Decoupling mitigates decoherence during idle periods

C) Twirling is for measurement errors, Dynamical Decoupling is for gate errors

D) Both techniques address the same types of errors but use different methods


<Details><summary>정답:</summary>
<br/>
Correct Answer: B
<br/>
Twirling converts coherent errors to incoherent noise, DD mitigates decoherence during idle periods *Pauli Twirling targets coherent gate errors by randomizing them into stochastic noise, while Dynamical Decoupling addresses qubit decoherence during idle times.

</Details>

---

**Question 44**

**What is the primary output format returned by the Sampler primitive's result() method?**

```
from qiskit.primitives import BackendSamplerV2 as Sampler
sampler = Sampler(backend=backend)
job = sampler.run([circuit], shots=100)
result = job.result()
# What type of object is 'result'?
```

A) A dictionary of counts like {'00': 50, '11': 50}

B) A PrimitiveResult object containing `PubResult` objects

C) A numpy array of measurement bitstrings

D) A list of probability values


<Details><summary>정답:</summary>
<br/>
Correct Answer: B
<br/>
The Sampler returns a structured result object: `PrimitiveResult($[SamplerPubResult(...)]$)`. Each PubResult contains a DataBin with the measurement data and metadata about the execution

</Details>

----

**Question 45**

**Which statement is correct about the code below?**

`sampler = Sampler(options={"resilience_level": 1})`

A) Resilience level 1 increases transpiler optimization level

B) Resilience level 1 enables full zero-noise extrapolation

C) Resilience level 1 enables basic error mitigation techniques

D) Resilience level 1 disables all noise suppression


<Details><summary>정답:</summary>
<br/>
Correct Answer: C
<br/>
resilience_level=1 enables basic error mitigation, such as readout error mitigation

</Details>

---

**Question 46**

**How do you access measurement results differently between Sampler V1 and V2?**

A) V1: `result.quasi_dists[0]` vs V2: `result[0].data.meas.get_counts()`

B) V1: `result[0].data.counts` vs V2: `result.counts[0]`

C) V1: `result.measurements[0]` vs V2: `result.data[0].counts`

D) V1: `result.get_counts(0)` vs V2: `result.counts(0)`


<Details><summary>정답:</summary>
<br/>
Correct Answer: A
<br/>
V1 returns quasi-probability distributions in quasi_dists, while V2 stores measurements in a BitArray object accessible via data.meas.

</Details>

---

**Question 47**

**Which of the following is the CORRECT Sampler V2 initialization?**

A)
```
from qiskit_ibm_runtime import Sampler as Sampler
sampler_v2 = Sampler(backend=backend)
```
B)
```
from qiskit_ibm_runtime import SamplerV2 as Sampler
sampler_v2 = Sampler(mode=backend)
```

C)
```
from qiskit_ibm_runtime import Sampler
sampler_v2 = Sampler(mode=backend) 
```

D)
```
from qiskit.primitives import SamplerV2 as Sampler
sampler_v2 = Sampler(backend=backend) 

```


<Details><summary>정답:</summary>
<br/>
Correct Answer: B
<br/>
V2 changed to mode= parameter: Sampler(mode=backend)

</Details>

---

**Question 48**

**What is the difference in how Sampler V1 and V2 structure their input parameters to the run() method?**

A) V1 uses separate arguments, V2 uses PUBs (Primitive Unified Blocs)

B) V1 uses PUBs, V2 uses separate arguments

C) Both use the same structure but with different parameter names

D) V1 requires circuits to be transpiled first, V2 transpiles automatically


<Details><summary>정답:</summary>
<br/>
Correct Answer: A
<br/>
V1 uses separate arguments, V2 uses PUBs (Primitive Unified Blocs) 
<br/>
V1: sampler.run(circuits=$[circuit]$)
<br/>
V2: sampler.run(pubs=$[(circuit,)]$)

</Details>

---

**Question 49**

**Which of the following correctly configures an Estimator with Zero-Noise Extrapolation (ZNE) using gate folding and 3 noise factors?**

```
from qiskit_ibm_runtime import EstimatorOptions as Options
options = Options()
```

A) 
```
Options.zne_enable = True
options.zne_amplifier = "gate_folding"
options.zne_factors = [1.0, 2.0, 3.0]
```

B) 
```
options.resilience.zne_mitigation = True
options.resilience.zne.amplifier = "gate_folding"
options.resilience.zne.noise_factors = [1.0, 2.0, 3.0]
```

C) 
```
options.error_mitigation.zne = True
options.error_mitigation.znemethod = "gate_folding"
options.error_mitigation.factors = [1.0, 2.0, 3.0]
```

D) 
```
options.set_zne(enable=True, method="gate_folding", factors=[1.0, 2.0, 3.0])
```


<Details><summary>정답:</summary>
<br/>
Correct Answer: B
<br/>
ZNE configuration is nested under `resilience.zne` with specific attribute names.
</Details>

---

**Question 50**

**What is the primary purpose of the resilience_level option in EstimatorOptions?**

A) To control the number of times a circuit is executed

B) To enable and coordinate different error mitigation techniques

C) To select the optimization level for circuit transpilation

D) To configure batch execution parameters


<Details><summary>정답:</summary>
<br/>
Correct Answer: B
<br/>
resilience_level (0-2) determines which error mitigation strategies (twirling, ZNE, measurement mitigation) are applied and how they're coordinated.
</Details>

---

**Question 51**

**Which error mitigation technique specifically targets measurement errors?**

A) Zero-Noise Extrapolation (ZNE)

B) Probabilistic Error Cancellation (PEC)

C) Dynamical Decoupling

D) Twirled readout error extinction (T-REx)


<Details><summary>정답:</summary>
<br/>
Correct Answer: D
<br/>
TREX is a measurement error mitigation technique that combines twirling with readout error correction
</Details>

---

**Question 52**

**What is the correct way to enable gate twirling with 16 randomizations in an Estimator?**

options = Options()

A)
```
options.twirling.enable_gates = True
options.twirling.num_randomizations = 16
```

B)
```
options.twirling.enable = True
options.twirling.randomizations = 16
```

C)
```
options.resilience.twirling = True
options.resilience.twirling_randomizations = 16
```

D)
```
options.gate_twirling = True
options.twirling_count = 16
```


<Details><summary>정답:</summary>
<br/>
Correct Answer: A
<br/>
Twirling options are configured via options.twirling, with specific attributes for gate and measurement twirling, and num_randomizations controls how many random samples are used

</Details>

---

**Question 53**

**Which of the following correctly initializes an Estimator with error suppression using dynamical decoupling on the "asap" schedule?**

A)
```
estimator = Estimator(mode=backend)
estimator.options.dynamical_decoupling = True
```

B)
```
estimator = Estimator(mode=backend)
estimator.resilience.dynamical_decoupling=True
estimator.resilience.dynamical_decoupling.scheduling_method="asap"

```

C)
```
estimator = Estimator(mode=backend)
estimator.options.dynamical_decoupling.enable = True
estimator.options.dynamical_decoupling.scheduling_method = "asap"
```

D)
```
estimator = Estimator()
estimator.enable_dynamical_decoupling("asap")
```


<Details><summary>정답:</summary>
<br/>
Correct Answer: C
<br/>
dynamical decoupling options are configured via options.dynamical_decoupling, with specific attributes for scheduling

</Details>

---

**Question 54**

**What is the difference between error suppression and error mitigation as described in the notebook?**

A) Error suppression happens during circuit execution while error mitigation happens after execution

B) Error suppression is only for measurement errors while error mitigation is for gate errors

C) Error suppression requires more qubits than error mitigation

D) Error suppression works on simulators only while error mitigation works on real hardware


<Details><summary>정답:</summary>
<br/>
Correct Answer: A
<br/>
error suppression includes techniques applied during circuit execution while error mitigation involves post-processing techniques.

</Details>

---

**Question 55**

**When using Probabilistic Error Amplification (PEA) with Zero-Noise Extrapolation, what is the correct order of stages?**

A) Extrapolation, Noise amplification, Learning

B) Learning, Extrapolation, Noise amplification

C) Learning, Noise amplification, Extrapolation

D) Noise amplification, Learning, Extrapolation


<Details><summary>정답:</summary>
<br/>
Correct Answer: C
<br/>
PEA consists of three stages Learning the noise model, noise amplification and then extrapolation
</Details>

---

**Question 56**

**Why is Pauli twirling considered an error suppression technique rather than error mitigation?**

A) Because it requires running the circuit multiple times to average results

B) Because it shapes coherent errors into stochastic errors during circuit execution

C) Because it uses post-processing to correct measurement outcomes

D) Because it extrapolates to the zero-noise limit mathematically


<Details><summary>정답:</summary>
<br/>
Correct Answer: B
<br/>
Error suppression techniques are applied during execution.
</Details>

---

**Question 57**

**Given the following code, which option correctly accesses the measurement counts from a Sampler result?**

```
from qiskit import QuantumCircuit
from qiskit_aer import AerSimulator
from qiskit.primitives import BackendSamplerV2 as Sampler

# Create and run a simple circuit
qc = QuantumCircuit(2)
qc.h(0)
qc.cx(0, 1)
qc.measure_all()

sampler = Sampler(backend=AerSimulator())
job = sampler.run([(qc,)], shots=100)
result = job.result()
pub_result = result[0]

```

A)
```
counts = pub_result.counts()
```

B)
```
counts = pub_result.data.get_counts()
```

C)
```
counts = pub_result.data.meas.get_counts()
```

D)
```
counts = pub_result.metadata['counts']
```


<Details><summary>정답:</summary>
<br/>
Correct Answer: C
<br/>
Use `pub_result.data.meas.get_counts()` to get measurments , if a register is specified use the register name instead of meas
</Details>

---

**Question 58**

**Given this job monitoring code, what is the main issue with it for monitoring cloud jobs?**

```
import time

job = sampler.run([(circuit,)])  # Cloud job submission
print(f"Job ID: {job.job_id()}")

while True:
    if job.status() == "DONE":
        print("Job completed!")
        result = job.result()
        break
    elif job.status() == "ERROR":
        print("Job failed!")
        break
    time.sleep(5)
```

A) The loop will run forever if the job gets stuck

B) It uses job.status() == "DONE" instead of job.done()

C) It doesn't handle the CANCELLED status

D) It doesn't use job.in_final_state() to check if polling should continue


<Details><summary>정답:</summary>
<br/>
Correct Answer: D
<br/>
while True loop will run indefinitely unless the job reaches either "DONE" or "ERROR" status

</Details>

---

**Question 59**

**Given the following code that runs an Estimator job to calculate expectation values, which option correctly accesses the expectation value result?**

```
estimator = Estimator(mode=backend)
job = estimator.run([(qc, observable)])
result = job.result()
pub_result = result[0]
```

A)`ev = pub_result.data.evs`

B)`ev = pub_result.value`

C)`ev = pub_result.data.evs[0]`

D)`ev = pub_result.metadata['expectation_value']`


<Details><summary>정답:</summary>
<br/>
Correct Answer: C
<br/>
Estimator stores expectation values in pub_result.data.evs, There is only one observable, so evs$[0]$ holds the result
</Details>

---

**Question 60**

**What is the primary purpose of the Target object in the BackendV2 interface?**

A) To store job execution results

B) To define the backend's physical capabilities and supported operations

C) To manage job queuing and prioritization

D) To handle user authentication with the provider


<Details><summary>정답:</summary>
<br/>
Correct Answer: B
<br/>
The target object holds the backend physical capabilities like supported gates, qubits, ...
</Details>

---

**Question 61**

**Which code correctly demonstrates creating multiple jobs within a Qiskit Runtime Session for efficient execution?**

A)
```
with Session(service=service, backend=backend) as session:
    sampler = SamplerV2(session=session)
    job1 = sampler.run([circuit1])
    job2 = sampler.run([circuit2])
```
B)
```
session = Session(service=service, backend=backend)
job1 = session.run([circuit1])
job2 = session.run([circuit2])
session.close()
```
C)
```
with Session(service=service, backend=backend) as session:
    sampler = SamplerV2()
    job1 = sampler.run([circuit1])
    job2 = sampler.run([circuit2])
```
D)
```
sampler = SamplerV2(backend=backend)
with Session(sampler=sampler) as session:
    job1 = sampler.run([circuit1])
    job2 = sampler.run([circuit2])
```



<Details><summary>정답:</summary>
<br/>
Correct Answer: A
<br/>
The correct pattern is create a Session, pass the session object to the primitive and then run multiple jobs using primitive
</Details>

---

**Question 62**

**Which of the following correctly shows how to retrieve session information after creating jobs within it?**

A)
```
session = Session(service=service, backend=backend)
print(f"Session backend: {session.backend}")
```
B)
```
with Session(service=service, backend=backend) as session:
    # ... run jobs ...
    print(f"Session ID: {session.id}")
    print(f"Session status: {session.get_status()}")
```   
C)
```
with Session(service=service, backend=backend) as session:
    sampler = SamplerV2(session=session)
    # ... run jobs ...
    print(f"Session ID: {session.session_id}")
    print(f"Session status: {session.status()}")
    print(f"Session usage: {session.usage()}")
```
D)
```
session = Session.create(service, backend)
print(f"Session details: {session.info()}")
```


<Details><summary>정답:</summary>
<br/>
Correct Answer: C
<br/>
Session information are stored in session,session_id, session_status() and session.usage()

</Details>

---

**Question 63**

**Given this code that monitors a job's lifecycle, which JobStatus values represent states where polling should continue?**

```
from qiskit.providers import JobStatus
import time

job = backend.run(circuit)

# Monitor job
while not job.in_final_state():
    status = job.status()
    print(f"Current status: {status.name}")
    time.sleep(2)

print("Job reached final state!")
```

A) QUEUED, VALIDATING, RUNNING, DONE

B) INITIALIZING, QUEUED, VALIDATING, RUNNING

C) DONE, ERROR, CANCELLED

D) RUNNING only


<Details><summary>정답:</summary>
<br/>
Correct Answer: B
<br/>
Jobs status is not in final state when it is INITIALIZING, QUEUED, VALIDATING or RUNNING
</Details>

---

**Question 64**

**What does the following code assign to my_pi?**


`angle[4] my_pi = π;`

A) Binary "1000" (8 units, where 1 unit = π/8)

B) Binary "0010" (2 units, where 1 unit = π/16)

C) Binary "1111" (15 units, where 1 unit = π/16)

D) Binary "1100" (12 units, where 1 unit = π/12)


<Details><summary>정답:</summary>
<br/>
Correct Answer: A
<br/>
For angle$[4]$, 1 unit = 2π / 2⁴ = π/8. π corresponds to 8 units, which in 4-bit binary is "1000".

</Details>

---

**Question 65**

**Which of the following is true about physical qubits in QASM?**

A) They are declared with physical_qubit

B) They can be indexed with $ notation and are predefined by hardware

C) They can be cast to virtual qubits using qubit($0)

D) They are only accessible inside hardware-specific subroutines


<Details><summary>정답:</summary>
<br/>
Correct Answer: B
<br/>
Physical qubits are referenced as $0, $1, etc., and are not declared—they are fixed by the hardware. They cannot be cast or treated like virtual qubits.
</Details>

---

**Question 66**

**What will be the output of this code?**

```
from qiskit.qasm3 import loads

qasm_code = """
OPENQASM 3.0;
include "stdgates.inc";

qubit[4] q;
bit[2] c;

h q[0:1];
cx q[0], q[2];
c[0] = measure q[1];
c[1] = measure q[3];
"""

circuit = loads(qasm_code)
print(f"Qubits: {circuit.num_qubits}, Clbits: {circuit.num_clbits}")
print(f"Operations: {len(circuit.data)}")
```

A) Qubits: 4, Clbits: 2, Operations: 4

B) Qubits: 4, Clbits: 2, Operations: 3

C) Qubits: 2, Clbits: 2, Operations: 3

D) Qubits: 4, Clbits: 2, Operations: 5


<Details><summary>정답:</summary>
<br/>
Correct Answer: D
<br/>
There are 4 qubits -> qubit$[4]$,  <br/>
2 classical bits -> bit$[2]$ and <br/>
5 operations -> h q$[0]$, h q$[1]$, cx q$[0]$, q$[2]$, measure q$[1]$ -> c$[0]$, measure q$[3]$ -> c$[1]$
</Details>

---

**Question 67**

**When exporting a circuit with Exporter(includes=$[]$), what is the effect?**

A) All standard gates will be included by default

B) The stdgates.inc file will not be included in the output

C) No gates will be exported

D) Only custom gate definitions will be included


<Details><summary>정답:</summary>
<br/>
Correct Answer: B
<br/>
The includes parameter specifies which files to include. An empty list means no includes, so stdgates.inc will not be added to the QASM3 output.

</Details>

---

**Question 68**

**What is the issue with this results retrieval code?**

```
import requests

job_id = "abc123"
reqUrl = f"https://quantum.cloud.ibm.com/api/v1/jobs/{job_id}/results"

headers = {
    "Accept": "application/json",
    "Authorization": "Bearer valid_token",
    "Service-CRN": "valid_crn"
}

response = requests.get(reqUrl, headers=headers)

if response.status_code == 200:
    print("Job completed successfully")
    print(response.json())
else:
    print(f"Error: {response.status_code}")
```

A) Missing IBM-API-Version header

B) Wrong endpoint for results

C) http 200 resposne status does not mean that the job completed successfully 

D) Missing Content-Type header


<Details><summary>정답:</summary>
<br/>
Correct Answer: C
<br/>
The code should check if the job status is completed before concluding that the job completed successfully.

</Details>